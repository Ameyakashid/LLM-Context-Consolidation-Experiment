# Vendored Source — google-calendar-mcp

This subtree is a **frozen** drop of an upstream third-party MCP server.
Do not modify its TypeScript, build scripts, or `package.json`. Re-sync by
replacing the subtree from the upstream repo at the pinned commit.

- Upstream: https://github.com/nspady/google-calendar-mcp
- Commit:   `0f2c9c5d7d96b63e424f035cca3269f857f3b0e1`
- Version:  `2.6.1`
- Vendored: 2026-04-18

## Byte-identity check

Verifier: `hashlib.sha256(Path("mcp/google-calendar/package.json").read_bytes())`
must match the same hash over `references/google-calendar-mcp/package.json`.

## Stripped from the drop

The following upstream metadata directories are intentionally absent and
must stay absent (they confuse the outer repo or leak tooling config):

- `.git/`  — upstream git metadata
- `.github/` — upstream CI workflows
- `.claude/` — upstream Claude Code settings

## Build artefacts (gitignored)

- `node_modules/` — `npm install` output
- `build/`        — `npm run build` output (esbuild bundle + web assets)
