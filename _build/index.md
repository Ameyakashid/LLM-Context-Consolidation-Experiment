# Project Index

> This file is the LLM-readable registry of everything built in this project. It follows the llms.txt standard. An LLM should read THIS FILE FIRST to understand what exists, then follow links to detail files for specifics.

## How To Use This Index

If you are an LLM agent in any IDE (Antigravity, Cursor, Claude Code, etc.) and you need to understand this project:
1. Read this file to find the component you need
2. Each entry has a semantic tag, a one-line summary, and a link to the full index report
3. Read the linked `x.md` file for file paths, exports, dependencies, and decisions
4. Read the linked source files if you need actual code

## Component Registry

(Entries appended by Verify agents after each subtask)

### Entry Format

```
### [semantic-tag]
> One-line description
- Status: DONE | DONE-WITH-ISSUES
- Task: NN/sub-NN
- Files: file1.py, file2.py, ...
- Depends on: [other-semantic-tag], [another-tag]
- Detail: _build/tasks/TASK_ID/sub-NN/NN-NNx.md
```

---

### [foundation-complete]
> Working nanobot-ai v0.1.5 workspace: build pipeline, Telegram bot config, multi-provider LLM (OpenRouter+Ollama), portable deployment, 39 passing tests
- Status: DONE-WITH-ISSUES
- Task: 01 (01-foundation)
- Subtasks: [build-scaffolding], [nanobot-workspace-setup], [bot-smoke-tests]
- Produces: Configured nanobot-ai workspace deployable to ~/.nanobot/, Telegram bot entry point, multi-provider LLM config (OpenRouter primary, Ollama fallback), build pipeline scaffolding (_build/ with plan, index, code-rules, 8 task specs). Downstream tasks 02-08 can now build on a running bot.
- Issues: (1) task-verify.md was not generated — no task-level cross-subtask verification exists. (2) code-rules.md has contradictory default parameter rule (lines 12 vs 58). (3) No mypy/pyright enforcement in dev dependencies. All LOW severity.
- Detail: _build/tasks/01-foundation/sub-03/01-03x.md (latest subtask; no task-verify.md)

### [build-scaffolding]
> Pipeline scaffolding: plan.md, index.md, code-rules.md, all 8 task specs, subtask descriptions — bootstrapped from PROJECT_BRIEF.md
- Status: DONE
- Task: 01/sub-01
- Files: _build/plan.md, _build/index.md, _build/code-rules.md, _build/tasks/01-foundation/spec.md, _build/tasks/01-foundation/sub-01/description.md, _build/tasks/01-foundation/sub-02/description.md, _build/tasks/02-personality/spec.md, _build/tasks/03-task-crud/spec.md, _build/tasks/04-memory/spec.md, _build/tasks/05-scheduling/spec.md, _build/tasks/06-buffer/spec.md, _build/tasks/07-voice/spec.md, _build/tasks/08-dashboard/spec.md
- Depends on: none
- Detail: _build/tasks/01-foundation/sub-01/01-01x.md

### [nanobot-workspace-setup]
> nanobot-ai v0.1.5 workspace with Telegram bot config, OpenRouter+Ollama multi-provider LLM, and portable deployment script
- Status: DONE
- Task: 01/sub-02
- Files: requirements.txt, setup_workspace.py, .env.example, .gitignore, workspace/SOUL.md, workspace/USER.md, workspace/HEARTBEAT.md, workspace/config.json.template, tests/test_setup_workspace.py
- Depends on: [build-scaffolding]
- Detail: _build/tasks/01-foundation/sub-02/01-02x.md

### [bot-smoke-tests]
> 18 smoke tests proving nanobot-ai config loads, provider resolves, and bot can start — validates sub-02 workspace setup
- Status: DONE
- Task: 01/sub-03
- Files: tests/test_bot_smoke.py
- Depends on: [nanobot-workspace-setup]
- Detail: _build/tasks/01-foundation/sub-03/01-03x.md

### [personality-core-complete]
> ADHD-native personality system: neuroaffirming SOUL.md, 6-state cognitive model (Baseline/Focus/Hyperfocus/Avoidance/Overwhelm/RSD) with Markov transitions, and nanobot-ai hook wiring state detection into per-message response adaptation — 212 tests passing
- Status: DONE-WITH-ISSUES
- Task: 02 (02-personality)
- Subtasks: [neuroaffirming-personality], [cognitive-state-detection], [state-response-integration]
- Produces: Complete personality layer for downstream tasks 03-08. SOUL.md loaded by nanobot-ai runtime with state-aware adaptation rules. StateResponseHook detects cognitive state per message and injects indicator into system prompt. States defined in editable YAML config. ICNU motivation framework and banned-phrase guardrails active. Disco Elysium personality voice stub ready for future milestone.
- Issues: (1) MEDIUM: normalize_llm_response substring fallback matches "focus" before "hyperfocus" — fix by sorting by length descending (state_detection.py:200-202). (2) task-verify.md was not generated — no task-level cross-subtask verification exists.
- Detail: _build/tasks/02-personality/sub-03/02-03x.md (latest subtask; no task-verify.md)

### [neuroaffirming-personality]
> SOUL.md personality definition with neuroaffirming rules, ICNU motivation framework, banned-phrase list, and AUDHD USER.md profile — plus 40 validation tests
- Status: DONE
- Task: 02/sub-01
- Files: workspace/SOUL.md, workspace/USER.md, tests/test_personality.py
- Depends on: [nanobot-workspace-setup], [bot-smoke-tests]
- Detail: _build/tasks/02-personality/sub-01/02-01x.md

### [cognitive-state-detection]
> 6-state cognitive model (Baseline/Focus/Hyperfocus/Avoidance/Overwhelm/RSD) with YAML config, LLM classification prompt, Markov transition enforcement, and StateName-typed function signatures — 132 tests across 4 files
- Status: DONE
- Task: 02/sub-02
- Files: workspace/states.yaml, state_detection.py, tests/test_state_config.py, tests/test_state_detection.py
- Depends on: [nanobot-workspace-setup], [neuroaffirming-personality]
- Issues: (1) MEDIUM: normalize_llm_response substring fallback matches "focus" before "hyperfocus" — fix by sorting by length descending. Low probability due to exact-match fast path.
- Detail: _build/tasks/02-personality/sub-02/02-02x.md

### [state-response-integration]
> Nanobot-ai hook detecting cognitive state per message, injecting indicator into system prompt, activating per-state SOUL.md response rules — 3 pure functions + StateResponseHook class, 40 tests
- Status: DONE
- Task: 02/sub-03
- Files: state_response_integration.py, workspace/SOUL.md, tests/test_state_response_pure.py, tests/test_state_response_hook.py
- Depends on: [neuroaffirming-personality], [cognitive-state-detection]
- Detail: _build/tasks/02-personality/sub-03/02-03x.md

### [task-crud-complete]
> Full task management pipeline: Pydantic data model + JSON-persisted TaskStore, 5 LLM-callable nanobot-ai Tool wrappers (create/list/get/update/complete), ADHD-friendly SOUL.md task guidance with 6-state cognitive awareness — 102 tests passing
- Status: DONE-WITH-ISSUES
- Task: 03 (03-task-crud)
- Subtasks: [task-data-model-store], [nanobot-task-tools], [soul-task-instructions]
- Produces: TaskStore CRUD API (task_store.py) and 5 registered Tool subclasses (task_tools.py) for downstream tasks 04-08. SOUL.md now includes Task Management section with state-aware presentation rules. Scheduling (task-05) and buffer system (task-06) can build on the Task model and store. Tools require programmatic registration via register_task_tools() at nanobot startup — config.json wiring not yet connected.
- Issues: (1) task-verify.md was not generated — no task-level cross-subtask verification exists. (2) LOW: apply_updates mixes JSON-mode and Python-mode dicts (task_store.py:95-98). (3) LOW: No file locking for concurrent TaskStore access (acceptable for single-user). (4) LOW: test helper `run()` missing type annotations (test_task_integration.py:58). (5) Tools not yet wired into nanobot startup — register_task_tools() call deferred to future integration.
- Detail: _build/tasks/03-task-crud/sub-03/03-03x.md (latest subtask; no task-verify.md)

### [task-data-model-store]
> Task Pydantic model + JSON-persisted TaskStore with full CRUD, atomic writes, pure helper functions — 39 tests
- Status: DONE
- Task: 03/sub-01
- Files: task_store.py, tests/test_task_model.py, tests/test_task_store.py
- Depends on: [nanobot-workspace-setup]
- Detail: _build/tasks/03-task-crud/sub-01/03-01x.md

### [nanobot-task-tools]
> Five LLM-callable nanobot-ai Tool subclasses wrapping TaskStore CRUD — create, list, get, update, complete — with JSON parameter schemas, programmatic registry, 34 tests
- Status: DONE
- Task: 03/sub-02
- Files: task_tools.py, tests/test_task_tools.py
- Depends on: [task-data-model-store], [nanobot-workspace-setup]
- Detail: _build/tasks/03-task-crud/sub-02/03-02x.md

### [soul-task-instructions]
> SOUL.md task management guidance (ADHD-friendly presentation, state-aware behavior) + 29 integration tests verifying full CRUD pipeline and persistence
- Status: DONE
- Task: 03/sub-03
- Files: workspace/SOUL.md, tests/test_task_integration.py
- Depends on: [neuroaffirming-personality], [task-data-model-store], [nanobot-task-tools], [cognitive-state-detection]
- Detail: _build/tasks/03-task-crud/sub-03/03-03x.md

### [memory-system-complete]
> Full memory pipeline: 5-category structured store, 3 LLM-callable tools (save/list/dismiss), context injection hook, SOUL.md memory guidance, MEMORY.md Dream seed — 90 tests across 6 files
- Status: DONE-WITH-ISSUES
- Task: 04 (04-memory)
- Subtasks: [memory-entry-store], [nanobot-memory-tools], [memory-context-injection]
- Produces: Complete memory layer for downstream tasks 05-08. MemoryEntryStore persists structured entries (JSON). Three nanobot-ai Tool subclasses expose CRUD to LLM. MemoryContextHook injects active entries into system prompt each message. SOUL.md teaches the bot when/how to use memory tools across 5 categories. MEMORY.md seeded for Dream's Phase 2 editor.
- Issues: (1) LOW: Double sort in MemoryContextHook._inject + format_memory_entries (redundant, not a bug). (2) LOW: HookContext protocol duplicated in memory_context.py and state_response_integration.py. (3) task-verify.md was not generated — no task-level cross-subtask verification exists.
- Detail: _build/tasks/04-memory/sub-03/04-03x.md (latest subtask; no task-verify.md)

### [memory-entry-store]
> JSON-persisted structured memory store — 5 categories (commitment/deadline/blocker/energy_state/context_switch), Pydantic model, soft-delete resolve, atomic writes — 43 tests
- Status: DONE
- Task: 04/sub-01
- Files: memory_store.py, tests/test_memory_model.py, tests/test_memory_store.py
- Depends on: [nanobot-workspace-setup]
- Detail: _build/tasks/04-memory/sub-01/04-01x.md

### [nanobot-memory-tools]
> Three LLM-callable nanobot-ai Tool subclasses wrapping MemoryEntryStore CRUD — save, list, dismiss — with JSON parameter schemas, ToolRegistry registration, 26 tests
- Status: DONE
- Task: 04/sub-02
- Files: memory_tools.py, tests/test_memory_tools.py
- Depends on: [memory-entry-store], [nanobot-workspace-setup]
- Detail: _build/tasks/04-memory/sub-02/04-02x.md

### [memory-context-injection]
> MemoryContextHook injecting active structured memories into system prompt + SOUL.md memory instructions for all 5 categories + MEMORY.md seed — 21 tests
- Status: DONE
- Task: 04/sub-03
- Files: memory_context.py, workspace/SOUL.md, workspace/memory/MEMORY.md, tests/test_memory_context.py, tests/test_memory_integration.py
- Depends on: [memory-entry-store], [state-response-integration]
- Detail: _build/tasks/04-memory/sub-03/04-03x.md

### [scheduling-complete]
> State-adaptive check-in scheduling: 4 check-in types, 6×4 cognitive-state evaluation matrix (fire/defer/modify/suppress), heartbeat hook integration, shared HookContext extraction, SOUL.md+HEARTBEAT.md guidance — 124 tests, hyperfocus substring bug fixed
- Status: DONE-WITH-ISSUES
- Task: 05 (05-scheduling)
- Subtasks: [checkin-schedule-engine], [state-aware-scheduling-logic], [scheduling-heartbeat-integration]
- Produces: Complete scheduling layer for downstream tasks 06-08. CheckInScheduleStore persists 4 configurable check-in types (morning_motivation, morning_plan, afternoon_check, evening_review) with staleness-guarded due detection. schedule_engine.py maps (check-in type × cognitive state) to actions via pure evaluate_checkin(). SchedulingHook reads enriched system prompt after StateResponseHook and MemoryContextHook, injects formatted check-in blocks into heartbeat sessions. hook_context.py provides shared HookContext Protocol for all 3 hooks. SOUL.md has per-type tone/content guidance. HEARTBEAT.md expanded to all 4 types. Hyperfocus substring ordering bug fixed in state_detection.py.
- Issues: (1) MEDIUM: test_schedule_engine.py (371 lines) and test_scheduling_hook.py (494 lines) both exceed 300-line limit — split recommended. (2) LOW: RSD handled as implicit else in evaluate_checkin — could add explicit check. (3) LOW: Unused timezone import in checkin_schedule.py. (4) task-verify.md was not generated — no task-level cross-subtask verification exists.
- Detail: _build/tasks/05-scheduling/sub-03/05-03x.md (latest subtask; no task-verify.md)

### [buffer-system-complete]
> Full buffer system for recurring obligations: Pydantic model + JSON-persisted BufferStore, 5 LLM-callable nanobot-ai tools (create/list/get/refill/decrement), auto-decrement heartbeat hook with low-level alert injection, ADHD-friendly SOUL.md+HEARTBEAT.md guidance — 147 tests passing
- Status: DONE-WITH-ISSUES
- Task: 06 (06-buffer)
- Subtasks: [buffer-data-model-store], [nanobot-buffer-tools], [buffer-heartbeat-hook]
- Produces: Complete buffer layer for downstream tasks 07-08. BufferStore persists pre-loaded units of recurring obligations (JSON). Five Tool subclasses expose CRUD+refill+decrement to LLM. BufferHook auto-decrements due buffers in heartbeat sessions and injects factual alerts (no guilt language) into system prompt when levels hit threshold. SOUL.md has 6-state cognitive-aware buffer guidance. HEARTBEAT.md has buffer monitoring section. Hook chain is now 4 deep: StateResponseHook → MemoryContextHook → SchedulingHook → BufferHook. Tools require programmatic registration via register_buffer_tools() at nanobot startup — config.json wiring not yet connected.
- Issues: (1) MEDIUM: test_buffer_model.py at 348 lines exceeds 300-line limit — split recommended. (2) LOW: apply_buffer_updates mixes JSON/Python-mode dicts (consistent with task_store.py). (3) LOW: BufferUpdate lacks Field constraints (caught at Buffer revalidation). (4) task-verify.md was not generated — no task-level cross-subtask verification exists.
- Detail: _build/tasks/06-buffer/sub-03/06-03x.md (latest subtask; no task-verify.md)

### [buffer-data-model-store]
> Buffer Pydantic model + JSON-persisted BufferStore with CRUD, decrement (level-1 + advance due date), refill (capped at capacity), atomic writes — 72 tests, verified PASS
- Status: DONE
- Task: 06/sub-01
- Files: buffer_store.py (285 lines), tests/test_buffer_model.py (348 lines), tests/test_buffer_store.py (266 lines)
- Depends on: [nanobot-workspace-setup]
- Issues: (1) MEDIUM: test_buffer_model.py at 348 lines exceeds 300-line limit — split recommended after TestBuildBuffer. (2) LOW: apply_buffer_updates mixes JSON/Python-mode dicts (same as task_store.py). (3) LOW: BufferUpdate lacks Field constraints (consistent with TaskUpdate).
- Detail: _build/tasks/06-buffer/sub-01/06-01x.md

### [checkin-schedule-engine]
> Check-in schedule data model (4 types), JSON-persisted store, pure due-check engine with staleness guard, + hyperfocus substring bug fix — 44 tests
- Status: DONE
- Task: 05/sub-01
- Files: checkin_schedule.py, tests/test_checkin_schedule.py, state_detection.py, tests/test_state_detection.py
- Depends on: [nanobot-workspace-setup], [cognitive-state-detection]
- Detail: _build/tasks/05-scheduling/sub-01/05-01x.md

### [state-aware-scheduling-logic]
> Pure decision layer: 6×4 state×check-in evaluation matrix (fire/defer/modify/suppress) + context assembly from TaskStore/MemoryEntryStore — 44 tests
- Status: DONE
- Task: 05/sub-02
- Files: schedule_engine.py, tests/test_schedule_engine.py
- Depends on: [checkin-schedule-engine], [cognitive-state-detection], [task-data-model-store], [memory-entry-store]
- Detail: _build/tasks/05-scheduling/sub-02/05-02x.md

### [scheduling-heartbeat-integration]
> Nanobot-ai hook wiring scheduling engine into heartbeat sessions — proactive check-in delivery with state-aware gating, SOUL.md guidance, shared HookContext extraction — 36 tests
- Status: DONE
- Task: 05/sub-03
- Files: hook_context.py, scheduling_hook.py, state_response_integration.py, memory_context.py, workspace/SOUL.md, workspace/HEARTBEAT.md, tests/test_scheduling_hook.py
- Depends on: [checkin-schedule-engine], [state-aware-scheduling-logic], [state-response-integration], [memory-context-injection], [task-data-model-store], [memory-entry-store]
- Detail: _build/tasks/05-scheduling/sub-03/05-03x.md

### [nanobot-buffer-tools]
> Five LLM-callable nanobot-ai Tool subclasses wrapping BufferStore (create/list/get/refill/decrement) + SOUL.md ADHD-friendly buffer guidance with 6-state cognitive awareness — 37 tests
- Status: DONE
- Task: 06/sub-02
- Files: buffer_tools.py, tests/test_buffer_tools.py, tests/test_buffer_format.py, workspace/SOUL.md
- Depends on: [buffer-data-model-store], [neuroaffirming-personality], [cognitive-state-detection]
- Detail: _build/tasks/06-buffer/sub-02/06-02x.md

### [buffer-heartbeat-hook]
> Auto-decrement hook for due buffers with low-level alert injection into system prompt — staleness guard via due-date advancement, ADHD-friendly factual alerts, HEARTBEAT.md buffer monitoring section — 38 tests
- Status: DONE
- Task: 06/sub-03
- Files: buffer_hook.py, tests/test_buffer_hook.py, tests/test_buffer_hook_lifecycle.py, hook_context.py, workspace/HEARTBEAT.md
- Depends on: [buffer-data-model-store], [nanobot-buffer-tools], [scheduling-heartbeat-integration]
- Detail: _build/tasks/06-buffer/sub-03/06-03x.md

### [voice-system-complete]
> Full voice output pipeline: Kokoro TTS engine (kokoro-onnx), WAV-to-OGG/Opus delivery via SpeakTool, state-aware auto-voice hook detecting check-in/buffer-alert triggers — 5-hook chain complete, env var toggle, SOUL.md+HEARTBEAT.md voice guidance — 116 tests
- Status: DONE-WITH-ISSUES
- Task: 07 (07-voice)
- Subtasks: [kokoro-tts-engine], [voice-delivery-speak-tool], [voice-trigger-hook]
- Produces: Complete voice layer for downstream task 08. tts_engine.py synthesizes text→WAV via kokoro-onnx ONNX Runtime. voice_delivery.py converts WAV→OGG/Opus via PyAV. SpeakTool wraps the full TTS→convert→Telegram send pipeline as an LLM-callable nanobot-ai Tool with MessageTool delegation for chat routing. VoiceHook (5th position in hook chain: StateResponse→MemoryContext→Scheduling→Buffer→Voice) detects check-in and buffer-alert headings in system prompt, evaluates a 6-state×2-trigger matrix, and injects Voice Delivery instruction blocks for the LLM. Voice is auto-triggered in baseline+avoidance for check-ins, baseline-only for buffer alerts; suppressed in focus/hyperfocus/overwhelm/RSD. User-initiated voice ("say that aloud") taught via SOUL.md direct SpeakTool invocation. VOICE_AUTO_ENABLED env var toggles auto-voice at runtime without restart. setup_workspace.py extended with portable model download (kokoro-v1.0.onnx + voices-v1.0.bin).
- Issues: (1) MEDIUM: test_voice_trigger_hook.py at 404 lines exceeds 300-line limit — split recommended. (2) LOW: task-verify.md was not generated — no task-level cross-subtask verification exists. (3) LOW: Tools require programmatic registration via register_voice_tools() at nanobot startup — config.json wiring not yet connected.
- Detail: _build/tasks/07-voice/sub-03/07-03x.md (latest subtask; no task-verify.md)

### [kokoro-tts-engine]
> Kokoro TTS wrapper (kokoro-onnx): synthesize_speech() takes text → returns WAV bytes, with portable model download in setup_workspace.py and lazy-loaded singleton — 35 tests
- Status: DONE
- Task: 07/sub-01
- Files: tts_engine.py, setup_workspace.py, requirements.txt, tests/test_tts_engine.py, tests/test_model_download.py
- Depends on: [nanobot-workspace-setup]
- Detail: _build/tasks/07-voice/sub-01/07-01x.md

### [voice-delivery-speak-tool]
> WAV-to-OGG/Opus conversion via PyAV + SpeakTool nanobot-ai Tool wrapping TTS→convert→Telegram send pipeline, with MessageTool delegation for routing — 31 tests
- Status: DONE
- Task: 07/sub-02
- Files: voice_delivery.py, voice_tools.py, tests/test_voice_delivery.py, tests/test_voice_tools.py, requirements.txt
- Depends on: [kokoro-tts-engine], [nanobot-workspace-setup]
- Detail: _build/tasks/07-voice/sub-02/07-02x.md

### [voice-trigger-hook]
> State-aware auto-voice hook: detects check-in/buffer-alert headings in system prompt, evaluates 6-state x 2-trigger matrix, injects Voice Delivery instruction block for LLM to use SpeakTool — env var toggle, SOUL.md+HEARTBEAT.md voice guidance — 50 tests
- Status: DONE
- Task: 07/sub-03
- Files: voice_trigger_hook.py, tests/test_voice_trigger_hook.py, workspace/SOUL.md, workspace/HEARTBEAT.md, .env.example
- Depends on: [scheduling-heartbeat-integration], [buffer-heartbeat-hook], [cognitive-state-detection], [voice-delivery-speak-tool]
- Detail: _build/tasks/07-voice/sub-03/07-03x.md

### [dashboard-complete]
> Always-on Fire Tablet dashboard: stdlib HTTP API (5 data endpoints + /config), dark-theme HTML/CSS/JS frontend (auto-refresh 30s), cognitive state persistence, Python launcher (start.py) — glanceable passive display for state, tasks, buffers, schedule, activity
- Status: DONE-WITH-ISSUES
- Task: 08 (08-dashboard)
- Subtasks: [dashboard-data-api], [dashboard-frontend], [dashboard-startup-integration]
- Produces: Complete dashboard surface for Fire Tablet. start.py is the single entry point launching nanobot gateway + dashboard server in parallel. dashboard_api.py serves 6 endpoints (state/tasks/buffers/schedule/activity/config) from existing stores with CORS. Frontend auto-refreshes at server-configured interval. cognitive_state_writer.py persists state from StateResponseHook to disk for dashboard consumption. SOUL.md teaches bot dashboard awareness. DASHBOARD.md documents Fire Tablet Silk browser setup. setup_workspace.py creates data/ dir. This is the final task — all 8 tasks complete.
- Issues: (1) MEDIUM: resolve_static_file path traversal — str.startswith bypassed by sibling dirs, fix with Path.is_relative_to(). (2) MEDIUM: read_cognitive_state() doesn't handle corrupt JSON — will crash /state endpoint. (3) LOW: fetchJSON doesn't check response.ok. (4) task-verify.md was not generated — no task-level cross-subtask verification exists.
- Detail: _build/tasks/08-dashboard/sub-03/08-03x.md (latest subtask; no task-verify.md)

### [dashboard-data-api]
> Read-only HTTP API (stdlib http.server) serving cognitive state, tasks, buffers, schedule, and activity feed as JSON — plus cognitive state persistence from StateResponseHook to disk
- Status: DONE-WITH-ISSUES
- Task: 08/sub-01
- Files: dashboard_api.py, cognitive_state_writer.py, state_response_integration.py, tests/test_dashboard_api.py, tests/test_cognitive_state_writer.py, .env.example
- Depends on: [task-data-model-store], [buffer-data-model-store], [checkin-schedule-engine], [memory-entry-store], [cognitive-state-detection], [state-response-integration]
- Issues: (1) MEDIUM: resolve_static_file path traversal — str.startswith prefix check bypassed by sibling dirs, fix with Path.is_relative_to(). (2) MEDIUM: read_cognitive_state() doesn't handle corrupt JSON, will 500 on /state. (3) MEDIUM: ROUTES dict dead code. (4) LOW: 5 unused imports in dashboard_api.py. (5) LOW: DASHBOARD_STATIC_DIR missing from .env.example.
- Detail: _build/tasks/08-dashboard/sub-01/08-01x.md

### [dashboard-frontend]
> Fire Tablet dark-theme dashboard: cognitive state banner, buffer gauges, tasks, schedule, activity feed — auto-refreshes 30s, static serving in dashboard_api.py, 39 tests passing
- Status: DONE
- Task: 08/sub-02
- Files: dashboard/index.html, dashboard/style.css, dashboard/app.js, dashboard_api.py, tests/test_dashboard_static.py, tests/test_dashboard_api.py
- Depends on: [dashboard-data-api], [cognitive-state-detection], [task-data-model-store], [buffer-data-model-store], [checkin-schedule-engine]
- Issues: (1) MEDIUM (pre-existing): str.startswith path traversal bypass in resolve_static_file — use is_relative_to(). (2) LOW: ROUTES dict + unused imports dead code. (3) LOW: fetchJSON doesn't check response.ok.
- Detail: _build/tasks/08-dashboard/sub-02/08-02x.md

### [dashboard-startup-integration]
> Dashboard config endpoint, Python launcher (start.py), SOUL.md awareness, Fire Tablet docs, setup_workspace.py data dir — wires dashboard into project startup
- Status: DONE
- Task: 08/sub-03
- Files: start.py, dashboard_api.py, dashboard/app.js, .env.example, workspace/SOUL.md, workspace/DASHBOARD.md, setup_workspace.py, tests/test_dashboard_integration.py
- Depends on: [dashboard-data-api], [dashboard-frontend], [nanobot-workspace-setup]
- Detail: _build/tasks/08-dashboard/sub-03/08-03x.md

### [bug-fixes-test-health-complete]
> Full bug fix and test suite health pass: verified 2 prior bug fixes, fixed 38 async test failures, split 3 oversized test files — 867 tests passing, 0 failures
- Status: DONE
- Task: 10 (10-bug-fixes)
- Subtasks: [state-detection-bugs-verified], [async-test-fixes], [test-file-splits]
- Produces: Clean test suite (867 pass, 0 fail), all test files under 300 lines, no dead code. Downstream tasks can trust test results.
- Detail: _build/tasks/10-bug-fixes/task-verify.md

### [async-test-fixes]
> Fixed 38 async test failures by replacing deprecated asyncio.get_event_loop().run_until_complete() with asyncio.run() in 4 test files
- Status: DONE
- Task: 10/sub-02
- Files: tests/test_memory_context.py, tests/test_memory_integration.py, tests/test_voice_tools.py, tests/test_voice_trigger_hook.py
- Depends on: [voice-trigger-hook], [memory-context-injection]
- Detail: _build/tasks/10-bug-fixes/sub-02/10-02x.md

### [custom-gateway-core]
> Custom nanobot gateway module: initializes all stores, creates 5 hooks in chain order, registers 14 tools, replicates stock gateway with hook/tool injection
- Status: DONE-WITH-ISSUES
- Task: 09/sub-01
- Files: custom_gateway.py, gateway_runner.py, hook_adapter.py, tests/test_custom_gateway.py
- Depends on: [state-response-integration], [memory-context-injection], [scheduling-heartbeat-integration], [buffer-heartbeat-hook], [voice-trigger-hook], [nanobot-task-tools], [nanobot-buffer-tools], [nanobot-memory-tools], [voice-delivery-speak-tool]
- Detail: _build/tasks/09-custom-gateway/sub-01/09-01x.md

### [test-file-splits]
> Split 3 oversized test files into 7 files (all under 300 lines), removed unused imports — no behavior changes
- Status: DONE
- Task: 10/sub-03
- Files: tests/test_schedule_engine.py, tests/test_schedule_engine_filters.py, tests/test_scheduling_hook.py, tests/test_scheduling_hook_pure.py, tests/test_scheduling_hook_content.py, tests/test_voice_trigger_hook.py, tests/test_voice_trigger_pure.py
- Depends on: [async-test-fixes]
- Detail: _build/tasks/10-bug-fixes/sub-03/10-03x.md

### [start-py-integration]
> Updated start.py to use custom gateway directly instead of subprocess — dashboard thread unchanged, clean shutdown preserved
- Status: DONE
- Task: 09/sub-02
- Files: start.py, gateway_runner.py
- Depends on: [custom-gateway-core]
- Detail: _build/tasks/09-custom-gateway/sub-02/09-02x.md

### [config-hardening]
> Hardened config template: sendProgress false, maxToolIterations 10, env-var timezone — prevents thinking leaks, runaway costs, timezone bugs
- Status: DONE
- Task: 09/sub-03
- Files: workspace/config.json.template, .env.example, setup_workspace.py, tests/test_bot_smoke.py, tests/test_setup_workspace.py
- Depends on: [nanobot-workspace-setup], [custom-gateway-core]
- Detail: _build/tasks/09-custom-gateway/sub-03/09-03x.md

### [custom-gateway-complete]
> Custom gateway with all hooks and tools wired: 5 hooks in chain order, 14 tools registered, subprocess replaced with direct call, config hardened — the ROOT CAUSE fix for personality/scheduling/buffers/voice not working
- Status: DONE-WITH-ISSUES
- Task: 09 (09-custom-gateway)
- Subtasks: [custom-gateway-core], [start-py-integration], [config-hardening]
- Produces: Working custom gateway that registers all hooks and tools built in tasks 02-07. start.py launches it directly. Config prevents thinking leaks, runaway costs, and timezone bugs. Downstream: Task 11 (Disco) can now add its hook to the chain.
- Issues: (1) MEDIUM: gateway_runner.py relies on 3 private nanobot APIs. (2) LOW: E2E testing requires nanobot installed.
- Detail: _build/tasks/09-custom-gateway/task-verify.md

### [disco-config-voices]
> Disco Elysium voice config: 4 voices (Volition/Empathy/Logic/Inland Empire), YAML definitions, state-conditional activation, env var toggle — cost-controlled at ~$0.18/month
- Status: DONE
- Task: 11/sub-01
- Files: disco_config.py, workspace/disco_voices.yaml, tests/test_disco_config.py
- Depends on: [cognitive-state-detection]
- Detail: _build/tasks/11-disco-flavor/sub-01/11-01x.md

### [disco-daisy-chain]
> 3-voice daisy chain engine: Volition first, each voice selects next, graceful partial results on error, injectable LLM callable — 31 tests
- Status: DONE
- Task: 11/sub-02
- Files: disco_engine.py, tests/test_disco_engine.py, tests/test_disco_chain.py
- Depends on: [disco-config-voices]
- Detail: _build/tasks/11-disco-flavor/sub-02/11-02x.md

### [disco-hook-integration]
> DiscoHook (6th in chain) using finalize_content to prepend inner voice commentary, conditional activation, SOUL.md disco awareness section
- Status: DONE
- Task: 11/sub-03
- Files: disco_hook.py, custom_gateway.py, workspace/SOUL.md, tests/test_disco_hook.py, tests/test_disco_hook_activation.py
- Depends on: [disco-daisy-chain], [disco-config-voices], [custom-gateway-core]
- Detail: _build/tasks/11-disco-flavor/sub-03/11-03x.md

### [disco-flavor-complete]
> Disco Elysium inner voice system: 4 voices, 3-voice daisy chain, state-conditional activation, finalize_content hook — adds personality commentary in avoidance/overwhelm/rsd states
- Status: DONE
- Task: 11 (11-disco-flavor)
- Subtasks: [disco-config-voices], [disco-daisy-chain], [disco-hook-integration]
- Produces: Complete inner voice layer. DiscoHook (6th in chain) runs 3-voice commentary chain after main response. Volition always speaks first, selects next voice. Only activates for emotional states (avoidance/overwhelm/rsd). Cost: ~$0.54/month. Configurable via YAML + env vars. SOUL.md teaches the LLM about the voices.
- Issues: (1) LOW: Tests can't run locally without nanobot installed. (2) LOW: No intent classification system yet for skip_intents.
- Detail: _build/tasks/11-disco-flavor/task-verify.md

### [pytest-collection-stabilization]
> Pytest now collects and passes the full suite from .venv: pytest.ini pins testpaths/pythonpath/norecursedirs, root conftest guards nanobot import with actionable error, requirements-dev.txt inherits runtime deps and adds pytest — 996 collected, 994 passed, 2 skipped, 0 errors
- Status: DONE
- Task: 12/sub-01
- Files: pytest.ini, conftest.py, requirements-dev.txt, tests/test_pytest_setup.py, README.md
- Depends on: [nanobot-workspace-setup], [async-test-fixes]
- Detail: _build/tasks/12-stabilization/sub-01/12-01x.md

### [workspace-deploy-memory-seed-and-model-pin]
> setup_workspace.py now deploys workspace/memory/MEMORY.md to ~/.nanobot/workspace/memory/ via pathlib subpath entries, and config.json.template pins the active model to x-ai/grok-4.1-fast (from openai/gpt-oss-120b) — byte-identical deploy verified, 997 passed / 2 skipped, zero runtime-path code changed
- Status: DONE
- Task: 12/sub-02
- Files: setup_workspace.py, workspace/config.json.template, tests/test_setup_workspace.py, tests/test_bot_smoke.py
- Depends on: [pytest-collection-stabilization], [nanobot-workspace-setup], [config-hardening], [memory-context-injection]
- Detail: _build/tasks/12-stabilization/sub-02/12-02x.md

### [nl-time-phrase-parser]
> Pure tz-aware natural-language time parser lifted from ReminderBot: parse_time_phrase(text, now) -> ParseResult | None with frozen dataclass (when/remaining_text/is_precise), required tz-aware now (ValueError on naive), mypy --strict clean, 33 tests (15 ported + 18 new), 1030 suite passing
- Status: DONE
- Task: 13/sub-01
- Files: nl_time_parser.py, tests/test_nl_time_parser.py, requirements.txt
- Depends on: [config-hardening]
- Detail: _build/tasks/13-nl-time-parsing/sub-01/13-01x.md

### [nl-time-task-tool-wiring]
> CreateTaskTool and UpdateTaskTool now accept ISO 8601 OR natural-language due_date via resolve_due_date(value, now) in task_time_helpers.py — ISO-first, NL-fallback, tz-aware via NANOBOT_TIMEZONE (defaults to UTC). Contract error `"could not parse time phrase '<value>'. Expected ISO 8601 … or natural-language phrase …"` is the terminal message for unrecognised input. SOUL.md teaches the LLM to pass phrases verbatim. task_tools.py split under the 300-line cap. 23 new tests, 1053 suite passing, mypy --strict clean on the new helper.
- Status: DONE-WITH-ISSUES
- Task: 13/sub-02
- Files: task_time_helpers.py, task_tools.py, tests/test_task_tools_nl_time.py, tests/test_task_tools.py, workspace/SOUL.md
- Depends on: [nl-time-phrase-parser], [nanobot-task-tools], [config-hardening]
- Issues: (1) MEDIUM: commit bundles unrelated SOUL.md Personality Voices → Disco Flavor Layer replacement (Task 11/sub-03 uncommitted work swept in) — violates AC #9's "no other sections modified" literally, zero behavioural impact. (2) LOW: tests/test_task_tools_registry.py holds the 3 registry-wiring tests moved out of test_task_tools.py but remains untracked in git. (3) LOW: .env.example documents `America/New_York` while code default is `UTC` when env var is unset — intentional asymmetry.
- Detail: _build/tasks/13-nl-time-parsing/sub-02/13-02x.md

### [nl-time-integration-clarification-lock]
> 13-test end-to-end integration suite locking the CreateTaskTool/UpdateTaskTool → resolve_due_date → parse_time_phrase path against frozen-`now` scenarios (happy-path NL, ISO regression, mixed sequence, update flow, self-correcting input, unparseable/empty clarification triggers, Friday-on-Friday ambiguity) + SOUL.md `### When Time Parsing Fails` subsection teaching the LLM to ask for restatement on parse failure (never silently drop due_date). 289-line test file, mypy --strict clean, 1053 → 1066 suite passing, zero regressions, frozen files (nl_time_parser.py, task_tools.py, task_time_helpers.py, task_store.py) untouched.
- Status: DONE
- Task: 13/sub-03
- Files: tests/test_task_tools_nl_integration.py, workspace/SOUL.md
- Depends on: [nl-time-phrase-parser], [nl-time-task-tool-wiring]
- Detail: _build/tasks/13-nl-time-parsing/sub-03/13-03x.md

### [code-hygiene-gateway-rules]
> Custom-gateway code-drift cleanup + code-rules mutable-default rule consolidation. `register_all_tools` returns a computed sum of four per-registrar counts (each registrar now returns `int`) instead of the hardcoded literal 13; `create_hooks` docstring now accurately describes "5 base hooks plus an optional DiscoHook"; `hook_adapter.py` carries a WHY comment explaining the log-and-swallow pattern; code-rules.md collapses the two overlapping mutable-default bullets into a single Universal Rules statement. Zero runtime behaviour change (same hooks, same tool counts, same log messages — now %d-formatted against computed counts). Test count-contract assertions locked at the per-registrar level so future regressions are caught where the drift would occur. 1066 passed, 2 skipped.
- Status: DONE
- Task: 12/sub-03
- Files: custom_gateway.py, hook_adapter.py, task_tools.py, buffer_tools.py, memory_tools.py, voice_tools.py, _build/code-rules.md, tests/test_custom_gateway.py, tests/test_task_tools_registry.py, tests/test_buffer_tools.py, tests/test_memory_tools.py, tests/test_voice_tools.py
- Depends on: [pytest-collection-stabilization], [custom-gateway-core], [disco-hook-integration], [nanobot-task-tools], [nanobot-buffer-tools], [nanobot-memory-tools], [voice-delivery-speak-tool]
- Detail: _build/tasks/12-stabilization/sub-03/12-03x.md

### [gcal-mcp-server-wiring]
> Vendor google-calendar-mcp TypeScript server at commit 0f2c9c5d (v2.6.1) under mcp/google-calendar/, wire into workspace/config.json.template as tools.mcpServers.google-calendar with ${ADHD_REPO_ROOT}/${GOOGLE_OAUTH_CREDENTIALS}/${GOOGLE_CALENDAR_MCP_TOKEN_PATH} placeholders, add gcal_setup.py (is_gcal_enabled, strip_gcal_mcp_server, build_google_calendar_mcp) for feature-flag-gated npm install/run build + 0700 token dir, extend setup_workspace.py to short-circuit when GOOGLE_CALENDAR_ENABLED=false and strip the MCP entry from the resolved config, .env.example gains 4 new entries (flag + 3 paths), .gitignore blocks node_modules/build/real-oauth-keys with gcp-oauth.keys.example.json negation. 39 new tests (28 build+vendor+gitignore+env, 11 template-shape+flag+secrets), 1105 passed / 2 skipped, mypy --strict clean on gcal_setup.py, setup_workspace.py, and both test files. package.json byte-identical to references/ drop (sha256 match). Upstream .git/.github/.claude dirs stripped.
- Status: DONE
- Task: 14/sub-01
- Files: gcal_setup.py, setup_workspace.py, workspace/config.json.template, .env.example, .gitignore, mcp/google-calendar/ (vendored tree, frozen), mcp/google-calendar/.vendor-source.md, tests/test_setup_workspace_gcal.py, tests/test_config_template_gcal.py
- Depends on: [nanobot-workspace-setup], [config-hardening], [workspace-deploy-memory-seed-and-model-pin], [pytest-collection-stabilization]
- Detail: _build/tasks/14-google-calendar-mcp/sub-01/14-01x.md

### [gcal-python-tool-wrappers]
> Three read-only Python Tool wrappers (`get_upcoming_events`, `list_events_in_window`, `check_free_busy`) over the vendored google-calendar MCP server: 60s TTL+FIFO `CalendarCache` keyed by canonical-JSON of input args (not derived timestamps), `CalendarMCPClient` that lazily resolves `mcp_google-calendar_<tool>` wrappers in the live ToolRegistry and returns `{"error": "calendar_unavailable"|"calendar_mcp_failure", "detail": ...}` JSON envelopes instead of raising, `register_calendar_tools(registry, cache, client) -> 3` wired into `custom_gateway.register_all_tools` behind `is_gcal_enabled(os.environ)`. Defense-in-depth read-only enforcement: `enabledTools: ["list-events", "get-freebusy"]` in workspace/config.json.template hides the 5 write-capable upstream tools at the nanobot layer; SOUL.md `## Calendar` section (between Dashboard and Disco Flavor Layer, +38 lines, surgical diff) teaches the LLM the read-only constraint and offers `create_task`/`save_memory` as the closest write alternative. Cache key uses inputs (hours_ahead, calendar_id, sorted calendar_ids) so identical-intent calls coalesce within the TTL window. mypy --strict clean on the 3 source files; 57 new tests (cache TTL/FIFO/eviction, client dispatch + envelope coercion, per-tool dispatch shapes + cache-key order insensitivity, registry wiring + write-name absence, env-flag matrix); pytest 1162 passed / 2 skipped.
- Status: DONE
- Task: 14/sub-02
- Files: calendar_cache.py, calendar_mcp_client.py, calendar_tools.py, custom_gateway.py, workspace/config.json.template, workspace/SOUL.md, tests/test_calendar_cache.py, tests/test_calendar_mcp_client.py, tests/test_calendar_tools.py, tests/test_calendar_tools_registry.py, tests/test_custom_gateway_gcal.py
- Depends on: [gcal-mcp-server-wiring], [custom-gateway-core], [code-hygiene-gateway-rules], [neuroaffirming-personality]
- Detail: _build/tasks/14-google-calendar-mcp/sub-02/14-02x.md

### [calendar-context-hook]
> Morning-only calendar-context injection closing Task 14: `CalendarContextHook` (chain position 4, after `SchedulingHook`, before `BufferHook`) detects `## Active Check-In: Morning Motivation|Morning Plan` headings in the system prompt and appends `### Today's Calendar` with up to 8 short event lines (24h times, location suffix, "nothing scheduled — free day" fallback). State-gated via `ALLOWED_STATES = {baseline, focus, avoidance, rsd}` — hyperfocus/overwhelm skip injection. Shares a `CalendarCache`/`CalendarMCPClient` instance with the sub-02 tools; `CalendarMCPClient.__init__(registry=None)` + `set_registry()` added for deferred wiring so the hook is constructable before `AgentLoop` builds its `ToolRegistry`. Structured error envelopes (`calendar_unavailable`, `calendar_mcp_failure`) and malformed payloads collapse to an `[Calendar unavailable — ... /calendar_auth or CALENDAR.md]` marker that the LLM voices per SOUL.md's new "When Calendar Is Unavailable" sub-subsection. WARNING logs rate-limited 1/hr per hook instance. New `workspace/CALENDAR.md` (127 lines) ships via `setup_workspace.TEMPLATE_FILES`. HEARTBEAT.md gets "Morning Check-Ins See Today's Calendar" section. Feature-flag OFF path costs zero (no cache, no client, hook not in chain). 71 new tests (43 pure-function, 16 hook-lifecycle, 5 chain-integration, 4 deploy, 3 gateway chain-order), 1233 passed / 2 skipped, mypy --strict clean on `calendar_hook.py` and the 4 new test files.
- Status: DONE-WITH-ISSUES
- Task: 14/sub-03
- Files: calendar_hook.py, custom_gateway.py, gateway_runner.py, calendar_mcp_client.py, setup_workspace.py, workspace/SOUL.md, workspace/HEARTBEAT.md, workspace/CALENDAR.md, tests/test_calendar_hook.py, tests/test_calendar_hook_format.py, tests/test_calendar_integration.py, tests/test_setup_workspace_calendar_md.py, tests/test_custom_gateway.py
- Depends on: [gcal-python-tool-wrappers], [gcal-mcp-server-wiring], [scheduling-heartbeat-integration], [buffer-heartbeat-hook], [voice-trigger-hook], [neuroaffirming-personality], [custom-gateway-core]
- Issues: (1) MEDIUM: tests/test_custom_gateway.py grew from 289 → 358 lines, breaking the 300-line cap (`code-rules.md:27`). Split out the three new calendar-chain tests or lift `TestRegisterAllTools` into its own file. (2) LOW: `UNAVAILABLE_LINE` hard-codes "authorization has expired" for all failure envelopes — misleads the user on non-OAuth failures (`calendar_mcp_failure`, parser errors). (3) LOW: `UNAVAILABLE_LINE` references `/calendar_auth` which has no slash-command handler wired (spec non-goal #3) — align with `CALENDAR.md`'s actual `npm run auth` flow. (4) LOW: broad `except Exception` in `before_iteration` swallows silently — matches hook-adapter convention but warrants a one-line WHY comment.
- Detail: _build/tasks/14-google-calendar-mcp/sub-03/14-03x.md

### [stabilization-cleanup]
> Final Task 12 cleanup: audit folder `bugs and issues with project/` moved on-disk to `_build/audits/2026-04-17/` (6 files), `.gitignore` gains `*.log` + `logs/` so `bot.log` and `logs/adhd_assistant.log` stop appearing as untracked (files remain on disk — `bot.log` is the 2026-04-16 runtime baseline), `REVERT_RESULTS.md` deleted (stale 2026-04-15 provider-switch scratch; content superseded by committed config.json.template + pinned memory). Regression gate: pytest 1066 passed / 2 skipped, AST-parse over 9 gateway/dashboard/start files clean, config.json.template JSON round-trip clean, setup_workspace.py deploys `memory/MEMORY.md` to throwaway HOME. Phase 2 baseline: test suite green, deploy complete, model pinned, code-rules reconciled, drift cleaned, audit archived, working tree clean. Only `.gitignore` entered git history (archive lives under gitignored `_build/`, by design — reversible with one `git add -f`).
- Status: DONE
- Task: 12/sub-04
- Files: .gitignore, _build/audits/2026-04-17/00-SUMMARY.md, _build/audits/2026-04-17/01-task09-gateway-audit.md, _build/audits/2026-04-17/02-task10-test-health.md, _build/audits/2026-04-17/03-task11-disco-audit.md, _build/audits/2026-04-17/04-missing-integrations.md, _build/audits/2026-04-17/05-smoke-test.md
- Depends on: [pytest-collection-stabilization], [workspace-deploy-memory-seed-and-model-pin], [code-hygiene-gateway-rules]
- Detail: _build/tasks/12-stabilization/sub-04/12-04x.md

### [magicmirror-vendor-and-config]
> Vendor MagicMirror² core (v2.35.0) + MMM-WebHookAlerts (v1.1.0) + MMM-Markdown (v1.0.0) + MMM-pages (v1.4.0) under `magicmirror/`; hand-authored `config/config.js.template` wires a 3-page class-based layout (`page0`=tasks, `page1`=state_buffers, `page2`=schedule, rotating 20s) with three webhook templates (`state_change`, `buffer_alert`, `missed_checkin`) in `fullscreen_above` and a fixed `clock`; `magicmirror_setup.py` exports `is_magicmirror_enabled`, `detect_node_npm`, `build_magicmirror`, `render_magicmirror_config`, `MAGICMIRROR_WEBHOOK_TEMPLATE_NAMES`, `MODULE_NAMES`, `MAGICMIRROR_CONFIG_VARS`; core `npm install` passes `--ignore-scripts` (defuses upstream `postinstall` `git clean -df`), module installs do not; freshness check compares `node_modules/` mtime to `package-lock.json` mtime; `render_magicmirror_config` substitutes 3 `${MAGICMIRROR_*}` placeholders with env→default fallback and writes gitignored `config.js`. `setup_workspace()` invokes `build_magicmirror` + `render_magicmirror_config` after the gcal flow, flag-gated by `MAGICMIRROR_ENABLED=false`. `.env.example` gains 6 entries (flag + host + port + ipWhitelist JSON + webhook host/port); `.gitignore` blocks `magicmirror/node_modules/`, `magicmirror/modules/MMM-*/node_modules/`, `magicmirror/config/config.js`. 4 `.vendor-source.md` pins with commit + version + stripped-dirs list. 63 new tests (21 setup + 42 template/vendor/gitignore/env), pytest 1296 passed / 2 skipped, mypy --strict clean. Flag-off default path costs zero (no npm, no filesystem work). Security gap documented: MMM-WebHookAlerts has no source-IP check; `ipWhitelist` widening to RFC1918 makes the webhook LAN-reachable rather than loopback-only — mitigation deferred to sub-03.
- Status: DONE-WITH-ISSUES
- Task: 15/sub-01
- Files: magicmirror_setup.py, setup_workspace.py, magicmirror/ (vendored tree), magicmirror/.vendor-source.md, magicmirror/modules/MMM-WebHookAlerts/ (vendored) + .vendor-source.md, magicmirror/modules/MMM-Markdown/ (vendored) + .vendor-source.md, magicmirror/modules/MMM-pages/ (vendored) + .vendor-source.md, magicmirror/config/config.js.template, .env.example, .gitignore, tests/test_magicmirror_setup.py, tests/test_magicmirror_config_template.py
- Depends on: [nanobot-workspace-setup], [config-hardening], [gcal-mcp-server-wiring], [pytest-collection-stabilization]
- Issues: (1) MEDIUM: `ipWhitelist` widened to RFC1918 LAN ranges exposes `POST /webhook` to any LAN device — spec says "reject requests not from the local machine." MMM-WebHookAlerts performs no source-IP check (delegates to MM Express `ipWhitelist`). Documented in module `.vendor-source.md`; mitigation deferred (would require patching the vendored module and breaking byte-identity, or a reverse-proxy rule in sub-03). (2) LOW: `strip_magicmirror_config` reserve-hook from description §2 omitted — MM config is not in `workspace/config.json.template`, so the hook would be empty (anti-slop rule #1). (3) LOW: `render_magicmirror_config` does not validate `MAGICMIRROR_PORT` is numeric — a typo produces invalid JS. (4) LOW: `_is_install_fresh` treats a missing `package-lock.json` as "fresh" — rare corner case, inverts to an unnecessary skip.
- Detail: _build/tasks/15-magicmirror-display/sub-01/15-01x.md

### [magicmirror-webhook-client-and-feeds]
> Pure-Python MagicMirror² feeders: `magicmirror_webhook.py` exports three frozen dataclass payloads (`StateChangePayload`, `BufferAlertPayload`, `MissedCheckinPayload`) whose `template_name` ClassVar is unpacked from `MAGICMIRROR_WEBHOOK_TEMPLATE_NAMES` (zero literal duplication, locked by source-grep test); each `to_json_body()` re-keys richer Python fields to the flat Mustache placeholders the vendored `config.js.template` actually renders (`{{state}}`, `{{buffer}}`, `{{level}}`, `{{capacity}}`, `{{checkin_type}}`), with a parametrized test that scrapes the template for every `{{key}}` and asserts body coverage — future template edits break the test instantly. `validate_local_url` accepts loopback (`127.0.0.0/8`, `localhost`, `::1`, `[::1]`) and rejects RFC1918 + public IPs + hostnames + URLs without host, message naming the rejected host. `send_alert_sync(payload, base_url, timeout=3.0) -> SendResult` POSTs via stdlib `urllib.request.urlopen` (no `httpx` dep) and folds three exception branches (`HTTPError`, `socket.timeout`, `URLError`) into a frozen `SendResult(ok, status_code, error, template_name)` — never raises on transport. `send_alert_async` submits to a module-level `ThreadPoolExecutor(max_workers=2)` lazy-initialized under a `Lock`, with `atexit.register(shutdown_webhook_pool)` (registered on first pool creation, not at import time, to keep imports side-effect-free); `shutdown_webhook_pool` swaps the global under the lock then calls `pool.shutdown(wait=False, cancel_futures=True)` outside the lock — idempotent and re-creates the pool on next `send_alert_async`. `magicmirror_feeds.py` exports three pure renderers (`render_tasks_markdown` groups active/completed-today/blocked sorted by due-date|updated_at|title with italic `_No …._` placeholders; `render_state_buffers_markdown` emits `## Cognitive state: {state}` + `## Buffers` with `(low)` plain-text marker at-or-below threshold; `render_schedule_markdown` distinguishes done-today/due-in/overdue/missed-today against caller-supplied tz-aware `now` — strips tz before combining with naive `entry.target_time`). Atomic `write_feeds(feed_dir, …)` writes each of the three files via `_atomic_write` (`Path.write_text(..., encoding="utf-8", newline="\n")` to a `.tmp` then `Path.replace` for cross-platform `os.replace` semantics) — interrupted writes leave prior feed files intact. `resolve_feed_dir(repo_root)` is the single source of truth for `magicmirror/modules/MMM-Markdown/markdown/`. "Blocked" task grouping uses `"blocked" in task.tags` (no `Task` model change needed). `render_state_buffers_markdown` accepts and `del`s `now` for caller-side signature symmetry. 6 new files, all under 300 lines (max 276); 63 new tests across 4 test files, full suite 1359 passed / 2 skipped (zero regressions); mypy --strict clean on all 6 new files. Sub-03 still owns: hook wiring, `MAGICMIRROR.md` user docs, gitignoring the three feed `.md` files, source-IP hardening on the webhook receiver (sub-01's MEDIUM M1 carries forward), and the implicit contract that callers pass `now` in `NANOBOT_TIMEZONE`.
- Status: DONE
- Task: 15/sub-02
- Files: magicmirror_webhook.py, magicmirror_feeds.py, tests/test_magicmirror_webhook_payloads.py, tests/test_magicmirror_webhook_client.py, tests/test_magicmirror_feeds_render.py, tests/test_magicmirror_feeds_write.py
- Depends on: [magicmirror-vendor-and-config], [task-data-model-store], [buffer-data-model-store], [checkin-schedule-engine], [cognitive-state-detection]
- Issues: (1) LOW: `_clean_webhook_pool` autouse fixture in `tests/test_magicmirror_webhook_client.py:76` is annotated `-> Any` instead of `-> Iterator[None]` — semantic-typing nit. (2) LOW: `render_schedule_markdown` strips `tzinfo` from `now` before combining with naive `entry.target_time`; renders correctly only when caller passes `now = datetime.now(ZoneInfo(NANOBOT_TIMEZONE))`. Contract is implicit; sub-03 should document or add an explicit `tzinfo` parameter. (3) LOW: `BufferAlertPayload.to_json_body()` emits `threshold` and `timestamp` not referenced by the current Mustache template — intentional auditability surface, not a bug.
- Detail: _build/tasks/15-magicmirror-display/sub-02/15-02x.md

### [magicmirror-hook-integration-and-docs]
> Closes Task 15 by wiring sub-02's webhook client + feed renderer into the live agent: `MagicMirrorHook` is slotted between `VoiceHook` and any `DiscoHook` via a new conditional branch in `hook_factory.create_hooks(repo_root, env)`. `before_iteration` gates on `is_scheduled_session()` (heartbeat-only, zero cost in chat turns) and runs four steps — `_dispatch_state_change`, `_dispatch_buffer_alerts`, `_dispatch_missed_checkins`, `_refresh_feeds` — each wrapped in per-step try/except with a rate-limited WARN (1/hr per hook instance via pure `should_log_error` predicate) so a broken webhook or disk-full cannot crash the agent loop. Dedup is in-memory (`_last_dispatched_state: StateName|None`, `_buffer_dispatched: set[(str, date)]`, `_missed_dispatched: set[(str, date)]`) — first heartbeat after restart primes state without dispatching; per-day keys mean a single duplicate after restart is the accepted tradeoff for avoiding disk-backed dedup. Buffer-alert source chose structured `buffer_store.list_active_buffers()` over prompt scraping (decouples from BufferHook's prompt format). Missed-check-in detection lives in a new pure predicate `is_checkin_missed(entry, current_date, current_datetime)` — inverse of `checkin_schedule.is_checkin_due`, strips tz from `current_datetime` before combining with naive `entry.target_time`. Feed refresh is per-heartbeat (unconditional) — calls the three sub-02 renderers + `write_feeds` (atomic tmp+rename). Webhook sends route through `send_alert_async` (fire-and-forget pool from sub-02). State-transition dispatch deviates from description: reads current state via closure from `StateResponseHook.current_state`, not from `cognitive_state_writer.read_cognitive_state()` — first-post-restart tick won't fire a state_change even if state differs across the restart. Hook-chain refactor fell out of the 300-line cap: `custom_gateway.py` (358 → 137 lines) re-exports `HOOK_CHAIN_ORDER`, `DISCO_HOOK_NAME`, `LLMCallableWrapper`, `SessionFlag`, `create_hooks` from the new `hook_factory.py` (260 lines) for back-compat. `tests/test_custom_gateway.py` split into that file (145 lines) + `tests/test_hook_factory.py` (175 lines). `workspace/MAGICMIRROR.md` (97 lines, 8 parts: TL;DR → Env Vars → How Alerts Work → Pages → When It Breaks → Turning It Off) ships via `setup_workspace.TEMPLATE_FILES`; byte-identical deploy test added. `workspace/SOUL.md` gains a surgical `## Fire Tablet Display` block between `## Dashboard` and `## Calendar` — four read-only bullets teaching the LLM to reference the mirror as a glanceable surface without prompting refreshes. `.gitignore` blocks `magicmirror/modules/MMM-Markdown/markdown/*.md` with a `!examples.md` negation for the vendored sample. `gateway_runner.py` now passes `repo_root=Path(__file__).resolve().parent` and `env=dict(os.environ)` to `create_hooks`. 51 new tests across 5 files (20 pure-helper + 11 hook-lifecycle + 5 chain-integration + 4 deploy + 11 moved hook-factory), full suite 1393 passed / 2 skipped, dashboard regression 52 passed, mypy --strict clean on `magicmirror_hook.py` (pre-existing `state_detection.py` errors unchanged). Flag-off path (`MAGICMIRROR_ENABLED≠true` or `repo_root` unset) allocates no hook, starts no thread pool, writes no feed files.
- Status: DONE-WITH-ISSUES
- Task: 15/sub-03
- Files: magicmirror_hook.py, hook_factory.py, custom_gateway.py, gateway_runner.py, setup_workspace.py, workspace/MAGICMIRROR.md, workspace/SOUL.md, .gitignore, tests/test_magicmirror_hook_pure.py, tests/test_magicmirror_hook.py, tests/test_magicmirror_integration.py, tests/test_magicmirror_docs_deploy.py, tests/test_hook_factory.py, tests/test_custom_gateway.py
- Depends on: [magicmirror-vendor-and-config], [magicmirror-webhook-client-and-feeds], [scheduling-heartbeat-integration], [buffer-heartbeat-hook], [voice-trigger-hook], [state-response-integration], [custom-gateway-core], [calendar-context-hook], [disco-hook-integration]
- Issues: (1) LOW: `gateway_runner.py` at 304 lines exceeds the 300-line cap (`code-rules.md:27`). File existed on disk at this size before this subtask but was untracked; commit 22cd4c6 committed+modified it. Fix: extract `_setup_cron_callback` (57 lines) into its own module. (2) LOW: Commit 22cd4c6 bundles Task 14/sub-03's uncommitted SOUL.md Calendar section (~43 lines) into the task-15-03 commit because it was in the working tree — the Fire Tablet Display insertion itself is surgical; the commit is not task-15-only. Mirrors Task 13/sub-02 bundling. Zero behavioural impact. (3) LOW: `_dispatch_state_change` primes `_last_dispatched_state` on first tick with no disk read — a state transition across a restart is silently absorbed. Description asked for `cognitive_state_writer.read_cognitive_state()` bootstrap; impl report documents the deviation as a deliberate tradeoff. (4) LOW: `MagicMirrorHook.before_iteration` has an outer `try/except` that duplicates `HookAdapter`'s built-in swallow+log; justified by WHY comment as enabling rate-limited context-aware logging. (5) LOW: 8 required ctor kwargs on `MagicMirrorHook` — matches `SchedulingHook` convention and has a single call site; a config dataclass is not warranted.
- Detail: _build/tasks/15-magicmirror-display/sub-03/15-03x.md

### [taskwarrior-store-backend]
> Drop-in Taskwarrior-CLI backend for `TaskStore`'s public API — vendored tasklib 2.5.1 + `TaskwarriorStore` class with full signature parity (8 methods), Option-A `+started`-tag status mapping, description-as-first-annotation field layout, `taskrc_location="/"` user-taskrc sandboxing, per-platform install-hint RuntimeError when `task` binary missing. Flag-gated via `TASKWARRIOR_ENABLED` (default false, no-op in sub-01) with optional `TASKWARRIOR_DATA_DIR` override; `setup_workspace.build_taskwarrior` creates `workspace/data/taskwarrior/` only when enabled, logs WARN with platform-specific install command when enabled-but-missing. Extracted to `taskwarrior_setup.py` (55 lines) so `setup_workspace.py` stays at 283/300. TW UUID adopted as `Task.id` (36-char form accepted by `Task.id: str` without model change; `build_task` hex discarded per U1 research decision). 68 platform-independent tests (23 pure-mapping + 19 signature-parity + 18 setup + 8 vendor-sanity) always run; 19 real-binary CRUD-parity tests mirror every `test_task_store.py` case and skip cleanly when `shutil.which("task")` is None. Full suite 1461 pass / 3 skip (+68 from Task 15/sub-03 baseline 1393); `mypy --strict` clean on both modules; `task_store.py` and `task_tools.py` unmodified. `.env.example` + `.gitignore` extended; `requirements.txt` path-installs `./vendor/tasklib`. Four LOW issues noted: (L1) `get_task` after `delete_task` returns the soft-deleted row instead of KeyError — parity gap, no current consumer; (L2) `tw_task_to_our_task` modified-fallback chain has redundant None check; (L3) `build_task` UUID generated then discarded in `create_task`; (L4) `reload()` docstring is WHAT not WHY; (L5) `taskrc_location="/"` has no explicit Windows real-binary test (dev box skips, Mac works by construction). None block; sub-02 can proceed.
- Status: DONE
- Task: 16/sub-01
- Files: taskwarrior_store.py, taskwarrior_setup.py, setup_workspace.py, requirements.txt, .env.example, .gitignore, vendor/tasklib/ (vendored tree), vendor/tasklib/.vendor-source.md, tests/test_taskwarrior_store.py, tests/test_taskwarrior_store_contract.py, tests/test_taskwarrior_store_mapping.py, tests/test_taskwarrior_store_setup.py, tests/test_tasklib_vendored.py
- Depends on: [task-data-model-store], [gcal-mcp-server-wiring], [magicmirror-vendor-and-config], [pytest-collection-stabilization]
- Detail: _build/tasks/16-taskwarrior-syncall/sub-01/16-01x.md

### [task-store-factory-and-migration]
> `TaskStoreProtocol` (`@runtime_checkable`, 8 methods) appended to `task_store.py` as the shared contract for JSON + Taskwarrior backends. `task_store_factory.build_task_store(env, repo_root) -> TaskStoreProtocol` is the single pinch-point: `TASKWARRIOR_ENABLED=true` ⇒ lazy-import `TaskwarriorStore` (JSON-only deploys pay zero `tasklib` cost, guarded by subprocess-based `sys.modules` regression test), else `TaskStore(storage_path=repo_root/workspace/data/tasks.json)`. No silent fallback when `task` CLI is missing — `RuntimeError` propagates. `TASKWARRIOR_DATA_DIR` absolute-path override honoured; `~` expanded. Consumers re-wired: `custom_gateway.create_stores(data_dir, repo_root=None, env=None)` (back-compat defaults so existing tests pass unchanged), `gateway_runner.py` threads `repo_root=Path(__file__).resolve().parent` + `env=os.environ`, `dashboard_api.handle_tasks` + `_build_activity_feed` route through the factory via new `_repo_root()` module helper, `hook_factory.create_hooks` narrows its `stores["task"]` cast to `TaskStoreProtocol`, `magicmirror_hook.MagicMirrorHook.__init__` + all 5 `task_tools.py` tool ctors + `register_task_tools` annotated with the Protocol (zero runtime change). `taskwarrior_setup.taskwarrior_data_dir_is_empty` + `warn_if_migration_needed(enabled, json_path, tw_data_dir) -> bool` emit a setup-time WARNING when flag-on ∧ `tasks.json` exists ∧ TW dir empty (missing/empty/zero-byte `pending.data` all count as empty). `setup_workspace.py` calls the warner after `build_taskwarrior`. `scripts/migrate_json_to_taskwarrior.py` (268 lines, `argparse` CLI) ships the one-shot reversible import: `--source` (JSON path), `--data-dir` (TW data dir), `--dry-run`, `--force`; idempotency marker is a `migrated_<first-8-hex>` tag (second run is a no-op); source JSON opened read-only and never modified; per-task round-trip diff on `{title, description, status, priority, tags, due_date}` (excludes `{id, created_at, updated_at}` by sub-01's UUID+tasklib-timestamp policy); `--force` required only when the target has unrelated non-migrated tasks; exit codes `0|2|3|4`; per-run summary appended to `<data-dir>/taskwarrior_migration.log`; rollback instructions printed on stdout on success. 52 new tests across 6 files (10 factory + 29 protocol-parity + 10 migration e2e + 3 gateway + 3 dashboard + 8 setup-warn), full suite 1512 passed / 8 skipped (Taskwarrior-CLI-gated files skip at collection time). `mypy --strict` clean on `task_store_factory.py` + `scripts/migrate_json_to_taskwarrior.py`. **Production note:** the factory's path resolution moves the canonical `tasks.json` location from `~/.nanobot/data/tasks.json` to `<repo>/workspace/data/tasks.json` — gateway + dashboard agree, but pre-existing installs need a one-time file move; flagged as MEDIUM in the verify report for deployment follow-up.
- Status: DONE
- Task: 16/sub-02
- Files: task_store.py, task_store_factory.py, task_tools.py, custom_gateway.py, gateway_runner.py, dashboard_api.py, hook_factory.py, magicmirror_hook.py, taskwarrior_setup.py, setup_workspace.py, scripts/migrate_json_to_taskwarrior.py, tests/test_task_store_factory.py, tests/test_task_store_protocol.py, tests/test_custom_gateway_taskwarrior.py, tests/test_dashboard_api_taskwarrior.py, tests/test_migrate_json_to_taskwarrior.py, tests/test_taskwarrior_store_setup.py, tests/test_custom_gateway.py, tests/test_dashboard_api.py, tests/test_dashboard_integration.py
- Depends on: [taskwarrior-store-backend], [custom-gateway-core], [dashboard-data-api], [magicmirror-hook-integration-and-docs], [task-crud-complete]
- Issues: (1) MEDIUM: canonical `tasks.json` location moved from `~/.nanobot/data/tasks.json` (ADHD_DATA_DIR/DASHBOARD_DATA_DIR) to `<repo>/workspace/data/tasks.json` — `dashboard_api.handle_tasks` now accepts but ignores its `data_dir` kwarg (factory uses `_repo_root()` instead). Existing deployments need a one-time file move; migration-warn logic does not announce this. (2) LOW: migration round-trip diff relaxes acceptance criterion 6 by excluding `{id, created_at, updated_at}` — driven by sub-01's UUID+tasklib-timestamp policy, linkage preserved via `migrated_<prefix>` tag. (3) LOW: migration run-log path deviates from description (`<data-dir>/taskwarrior_migration.log` vs `workspace/data/taskwarrior_migration.log`) — deliberate co-location. (4) LOW (informational): `task_tools.py` (303) and `gateway_runner.py` (303) sit at the 300-line cap — pre-existing violations, not introduced by sub-02.
- Detail: _build/tasks/16-taskwarrior-syncall/sub-02/16-02x.md

### [syncall-gcal-daemon]
> Flag-gated long-running daemon bidirectionally syncing Taskwarrior ↔ a named Google Calendar via the vendored `references/syncall` tree (commit `14a2615`, frozen under `vendor/syncall/`). Install strategy deviates from the usual "vendor + pip install" pattern: syncall's upstream `pyproject.toml` uses `poetry-dynamic-versioning` (no git history in a vendored subtree ⇒ build failure) and pins PyYAML `~5.3.1` which conflicts with the repo's `pyyaml>=6.0`; instead, `conftest.py` + the daemon both prepend `vendor/syncall/` to `sys.path` and `requirements.txt` pins 10 transitive deps individually (`taskw-ng`, `xdg`, `google-api-python-client`, `google-auth-oauthlib`, `bidict`, `click`, `loguru`, `rfc3339`, `item-synchronizer`, `bubop`). Code split: `syncall_args.py` (182 lines, 100% pure — `SyncallArgsConfig` dataclass, `read_syncall_args_config(env)`, `build_syncall_args(config)`, `resolve_resolution_strategy` dual-accepting friendly aliases and upstream class names, `resolve_verbosity_tokens`); `syncall_setup.py` (152 lines — `is_syncall_enabled`, `resolve_syncall_paths`, `build_syncall`, `write_repo_scoped_taskrc` — writes repo-scoped taskrc at `workspace/data/syncall_cache/taskrc` so `~/.taskrc` is never touched); `syncall_daemon.py` (289 lines — preflight → signal-handled while-loop → per-tick `subprocess.run([sys.executable, "-m", "syncall.scripts.tw_gcal_sync", ...])` with layered env `TASKRC`+`TASKDATA`+`XDG_CONFIG_HOME`+`PYTHONPATH`, `SIGINT`/`SIGTERM`/`SIGBREAK` handlers flip a threading.Event, sleep is 1-second-sliced when `sleep_fn is time.sleep` so shutdown is prompt). Conflict resolution: `SYNCALL_RESOLUTION_STRATEGY=tw_wins` (default) → `--resolution-strategy AlwaysSecondRS` (Taskwarrior canonical, per spec's Phase-2 rationale); aliases `tw_wins | gcal_wins | most_recent | least_recent` map to the four upstream `*RS` class names. Poll cadence: 600s default, 60s minimum, env-configurable via `SYNCALL_POLL_SECONDS`. Additive `SYNCALL_TW_FILTER` env var mitigates sub-02's `migrated_<hex>`-tag flood risk (example `"-migrated"` documented in SYNCALL.md). `start.py` restructured: gateway now runs in-process via `gateway_runner.run_gateway()` (replacing the prior `subprocess.Popen([python, "-m", "nanobot", "gateway"])`), syncall daemon spawned as a child `subprocess.Popen` when `is_syncall_enabled(os.environ)`, shutdown via `proc.terminate()` → `wait(15s)` → `kill()`. Popen constructor is an injectable parameter so `test_start_py_syncall.py` uses a `_StubPopen` without real forks. OAuth pickle location accepted as upstream's hardcoded `~/.gcal_credentials.pickle` (no flag/env redirect exists; patching the vendor would break byte-identity); belt-and-braces `.gitignore` pattern added. 74 new tests across 7 files (17 pure args + 16 setup + 17 daemon + 5 vendor + 7 env + 6 start.py + 7 setup_workspace), full suite 1586 passed / 11 skipped (zero regressions from sub-02 baseline 1512); mypy --strict clean on all three new source files. Flag-off default path is zero-cost (no subprocess, no filesystem work). `workspace/SYNCALL.md` (152 lines) deployed via `TEMPLATE_FILES` — covers TL;DR, prerequisites, one-time OAuth ritual, conflict policy, new-event-in-GCal implicit-creation behaviour, poll cadence, healthy-log examples, troubleshooting (including the `SYNCALL_TW_FILTER="-migrated"` mitigation and the home-dir pickle explanation).
- Status: DONE-WITH-ISSUES
- Task: 16/sub-03
- Files: syncall_args.py, syncall_setup.py, syncall_daemon.py, start.py, setup_workspace.py, conftest.py, requirements.txt, .env.example, .gitignore, workspace/SYNCALL.md, vendor/syncall/ (vendored tree), vendor/syncall/.vendor-source.md, tests/test_syncall_args.py, tests/test_syncall_setup.py, tests/test_syncall_daemon.py, tests/test_syncall_vendored.py, tests/test_syncall_env_example.py, tests/test_start_py_syncall.py, tests/test_setup_workspace_syncall.py
- Depends on: [taskwarrior-store-backend], [task-store-factory-and-migration], [gcal-mcp-server-wiring], [magicmirror-vendor-and-config], [custom-gateway-core]
- Issues: (1) MEDIUM: `syncall_daemon._preflight` only imports the `syncall` namespace, not `syncall.scripts.tw_gcal_sync`. If transitive deps (`bubop`, etc.) are missing, the daemon boots past preflight and logs `sync FAILED code=1` every poll interval instead of exiting 2 as acceptance criterion #4 promises. Fix: add `import syncall.scripts.tw_gcal_sync` to the preflight, or run `--help` in a subprocess with timeout. (2) MEDIUM: Windows shutdown is a hard kill — `start.py:stop_syncall_daemon` uses `proc.terminate()` which maps to `TerminateProcess` on Windows (SIGKILL equivalent), bypassing the daemon's SIGTERM/SIGBREAK handlers. On macOS (deployment target) the path is clean; on Windows, sub-04's integration test should verify graceful shutdown via `CTRL_BREAK_EVENT` + `CREATE_NEW_PROCESS_GROUP`. (3) LOW: `install_signals: bool = True` on `main()` is a flag parameter — code-rules forbids them. Defensible for testability but cleaner extracted to a wrapper. (4) LOW: `tests/test_syncall_daemon.py:165` has a dead `patch("syncall_daemon.importlib", create=True)` — the daemon never touches `importlib`. (5) LOW: `start.py` also swapped the gateway from `subprocess.Popen([python, "-m", "nanobot", "gateway"])` to in-process `run_gateway(None, None)` — a behavioural change outside the subtask description. All pre-existing tests still pass but it deserves a separate commit / review.
- Detail: _build/tasks/16-taskwarrior-syncall/sub-03/16-03x.md

### [taskwarrior-migration-finalization]
> Closes Task 16 by proving, documenting, and rollback-locking the Taskwarrior backend swap. Cross-consumer integration suite (`tests/test_task16_integration.py`, 10 tests, module-skipped when `task` CLI absent) exercises the three production seams end-to-end against a real Taskwarrior binary: (a) `ToolRegistry.execute(create_task|list_tasks|get_task|update_task|complete_task)` via `register_task_tools(registry, tw_store)`, (b) `dashboard_api.handle_tasks` with `_repo_root` monkey-patched to the tmp repo and `TASKWARRIOR_ENABLED=true`+`TASKWARRIOR_DATA_DIR` env-set, (c) `MagicMirrorHook.before_iteration(ctx)` with the feed byte-matching `render_tasks_markdown(tw_store.list_tasks(), fixed_now)`. Cross-path coherence test proves a single tool-registry `create_task` is visible through both the dashboard endpoint and the MagicMirror `tasks.md` feed. `tests/test_task_store_rollback.py` (5 tests + 1 CLI-gated skip) locks AC #11 — with `TASKWARRIOR_ENABLED` missing or explicitly `"false"`, `build_task_store(env, repo_root)` returns a `TaskStore` instance; `workspace/data/tasks.json` remains readable post-migration; flag-on with CLI available returns `TaskwarriorStore`. `tests/test_magicmirror_hook_taskwarrior.py` (2 CLI-gated tests) isolates the MagicMirror data-source switch: tasks.md output equals `render_tasks_markdown(tw_store.list_tasks(), FIXED_NOW)` byte-for-byte, and newly-added rows appear on the next tick. `workspace/TASKWARRIOR.md` (138 lines) is the user-facing doc — 9 ordered sections (TL;DR → Why → Prerequisites → First-time setup → Operation → Migration script → Rollback → Interaction with syncall → Troubleshooting); carries a triple-redundant divergence warning ("Divergence warning." bold sub-heading + "Do not flip-flop the flag" + "Pick a backend and stay there") that `tests/test_taskwarrior_md_deploy.py` asserts by grep. Ships via `setup_workspace.TEMPLATE_FILES` (entry index 6, byte-identical after `copy_workspace_files`). `workspace/SOUL.md` gains a surgical `## Task Ledger` subsection (9 body lines) between the existing `## Fire Tablet Display` and `## Calendar` headings — placement deviates from the description's literal wording ("between Calendar and Fire Tablet Display") because the live file order is FTD→Calendar; the impl chose the non-regressing adjacency-preserving slot and documented the deviation. Section tells the LLM: backend may be TW or JSON, tool behaviour is identical, do not surface the switch unless asked, point users at TASKWARRIOR.md / SYNCALL.md, recognise the `Taskwarrior CLI` RuntimeError and direct users to the install doc. `task_store.py` gets a 6-line WHY prologue prepended to the module docstring ("JSON backend — fallback when `TASKWARRIOR_ENABLED=false`. Post-Task-16 the canonical task backend is `taskwarrior_store.TaskwarriorStore`...") — zero behavioural change, 249 → 255 lines. `_build/tasks/16-taskwarrior-syncall/task-verify.md` stub records the four-subtask closure, canonical-backend identity, sync daemon status, rollback procedure, divergence warning, and Phase-2 baseline lock. 29 new tests total (6 deploy + 5 SOUL + 6 rollback + 2 MM+TW + 10 integration), full suite 1602 passed / 14 skipped / 0 regressions from sub-03 baseline of 1586. `mypy --strict` clean on every sub-04-touched file (`task_store.py`, `setup_workspace.py`, all 5 new test files); pre-existing `task_tools.py` / `dashboard_api.py` / `state_detection.py` errors are sub-02/earlier LOWs, not sub-04 regressions. Scope discipline intact: no edits to `taskwarrior_store.py`, `task_store_factory.py`, `syncall_*.py`. Phase-2 constraint (2026-04-16 clean-run baseline with both flags off) is byte-identically preserved.
- Status: DONE
- Task: 16/sub-04
- Files: workspace/TASKWARRIOR.md, workspace/SOUL.md, task_store.py, setup_workspace.py, tests/test_task16_integration.py, tests/test_taskwarrior_md_deploy.py, tests/test_soul_task_ledger_section.py, tests/test_task_store_rollback.py, tests/test_magicmirror_hook_taskwarrior.py, _build/tasks/16-taskwarrior-syncall/task-verify.md
- Depends on: [taskwarrior-store-backend], [task-store-factory-and-migration], [syncall-gcal-daemon], [magicmirror-hook-integration-and-docs], [task-crud-complete], [dashboard-data-api]
- Issues: (1) LOW: `tests/test_task_store_rollback.py:55-56` — test method `test_syncall_defaults_off` actually asserts `is_taskwarrior_enabled({}) is False`; name misleads, assertion is correct. Rename to `test_taskwarrior_defaults_off`. (2) LOW: `workspace/SOUL.md:279` placement is between `## Fire Tablet Display` and `## Calendar`; description text says "between Calendar and Fire Tablet Display" but live file ordering is FTD→Calendar. Impl decision is correct (non-regressing); description wording is wrong, not sub-04. (3) LOW: `tests/test_task16_integration.py:54-65` — `tw_env` fixture both returns a dict and monkeypatches env; the returned dict is redundant for fixtures consuming it only indirectly. (4) LOW: impl report's line-count table is slightly stale (claims 247 lines for integration test vs actual 290; 11 tests vs actual 10). Cosmetic. All under caps.
- Detail: _build/tasks/16-taskwarrior-syncall/sub-04/16-04x.md

### [pulse-engine-core]
> TEMM1E Pulse async timer engine + Schedule data model ported verbatim from `references/temm1e/crates/temm1e-perpetuum/src/pulse.rs` + `types.rs` into pure Python. Produces `pulse_schedule.py` (159 lines: Pydantic-discriminated `Schedule` union over `ScheduleAt` / `ScheduleEvery` / `ScheduleCron`, internally-tagged `kind` field; `PulseEvent(kind="concern_due", concern_id)`; pure functions `next_fire_time(schedule, tz)` and `next_fire_after(schedule, after, tz)` with `croniter` for 5-field cron parsing and `zoneinfo.ZoneInfo` for DST-aware wall-clock → UTC conversion; naive-datetime guard on `next_fire_after.after`; invalid cron → `None`) and `pulse_engine.py` (182 lines: `Pulse.create(store, cancel) → (Pulse, Queue[PulseEvent])` classmethod mirroring Rust's `Pulse::new` tuple return; `run()` loop races three tasks via `asyncio.wait(FIRST_COMPLETED)` over `cancel.wait()` / `_sleep_until_next()` / `_schedule_changed.wait()`, cancels losers, re-raises non-CancelledError task exceptions; `_query_next_fire_time` wraps `store.next_fire_time()` in `asyncio.wait_for(timeout=STORE_QUERY_TIMEOUT_S=10.0)` and returns None on timeout; `_fire_due_concerns` logs-and-continues on `claim_due_concerns` exceptions; `IDLE_POLL_SECONDS=60.0` when no concerns scheduled; `CHANNEL_MAX_SIZE=64`; `schedule_notifier() → asyncio.Event` stable identity). `PulseStoreProtocol` (structural, not `@runtime_checkable`) is the sole contract for sub-02 — two awaitable methods `next_fire_time() → datetime | None` and `claim_due_concerns(now) → list[ConcernId]`. 29 new tests (20 pure + 9 async using `asyncio.run`/`wait_for` not pytest-asyncio, with `FakePulseStore` / `RaisingStore` conforming structurally): ports all three Rust `mod tests` cases, adds DST spring-forward (2026-03-08 02:00 PST → 03:00 PDT via `next_fire_after(ScheduleCron("0 2 * * *"), 01:30 PST, LA)` asserting `10:00 UTC`), JSON round-trip per variant, frozen-instance enforcement, `every_seconds > 0` validation, async cancel-before-run (< 1s), cancel-mid-run, idle-poll cadence (monkeypatched `IDLE_POLL_SECONDS=0.02`), due-concern emission, schedule-change wake (verifies re-query without fire), store-timeout survival, store-exception swallow for `claim_due_concerns`, notifier identity stability, queue-maxsize. `requirements.txt` pins `croniter==6.2.2` (was transitive). Full suite 1631 passed / 14 skipped (baseline + 29 new, zero regressions); `mypy --strict` clean on all 4 files. Deviations from Rust documented in impl §4 (D1 JSON internally-tagged vs externally-tagged serde; D2 `cron5_to_cron7` omitted — croniter accepts 5-field natively; D3 `_query_next_fire_time` extracted for readability; D4 no channel-close detection — `asyncio.Queue.shutdown` needs 3.13, project is 3.12; D5 generic `Exception` in `_fire_due_concerns` swallow — narrow type comes from sub-02's store adapter; D6 `Pulse.create` classmethod replaces Rust's tuple-returning `new`; D7 explicit `ValueError` on naive `after` datetime). Engine is **inert** — importable but no caller outside `tests/`; `checkin_schedule.py` / `schedule_engine.py` / `scheduling_hook.py` / `custom_gateway.py` / `hook_factory.py` byte-identical to pre-subtask. Feeds sub-02 (Pulse store adapter conforming to `PulseStoreProtocol`) and sub-03 (hook/gateway cutover).
- Status: DONE-WITH-ISSUES
- Task: 17/sub-01
- Files: pulse_schedule.py, pulse_engine.py, tests/test_pulse_schedule.py, tests/test_pulse_engine.py, requirements.txt
- Depends on: [scheduling-complete]
- Issues: (1) MEDIUM: `pulse_engine.py:123-135` `_query_next_fire_time` catches only `asyncio.TimeoutError`; Rust `pulse.rs:70-73` uses `.unwrap_or(Ok(None)).unwrap_or(None)` which swallows store-side `Result::Err` as well. If sub-02's store raises a non-timeout exception from `next_fire_time()`, it propagates through `_sleep_until_next` → `_reraise_task_errors` → kills `Pulse.run`. Rust equivalent logs-and-continues. Not documented as deviation D8. Fix: broaden except or add deviation note. (2) MEDIUM: `pulse_schedule.py:40-46, 113-114` `ScheduleAt.at_utc` accepts naive `datetime` silently; `next_fire_time` then hits `TypeError: can't compare offset-naive and offset-aware datetimes` at call time. Asymmetric with `next_fire_after`'s explicit naive-guard (line 135-139). Fix: add Pydantic field-validator rejecting naive `at_utc`. (3) LOW: `ScheduleEvery.every_seconds = Field(gt=0)` tightens Rust's `Duration::from_secs(0)` legality; sensible but undocumented deviation. (4) LOW: `_cron_next_strictly_after` catches only `(CroniterBadCronError, ValueError)` — other croniter exception types (e.g. `CroniterNotAlphaError`) would escape instead of returning None.
- Detail: _build/tasks/17-temm1e-pulse-dream/sub-01/17-01x.md

### [pulse-checkin-store-adapter]
> Bridges sub-01's `PulseStoreProtocol` contract to the existing `CheckInScheduleStore` so the Pulse engine (when flag-on in sub-03) can fire the bot's four check-ins at their `target_time` in the `NANOBOT_TIMEZONE`. `pulse_checkin_store.py` (178 lines, side-effect free): `PulseCheckinStore(store, tz, now_provider=datetime.now(UTC))` — `next_fire_time()` iterates `store.list_entries()`, drops `is_enabled=False`, emits `now_utc` when `is_checkin_due(entry, local_date, local_time)` is already True (retroactive-fire-within-staleness collapse so Pulse's `_sleep_until_next` zero-sleeps and the hook fires on the same tick the bot boots, matching legacy `SchedulingHook` semantics) else materialises `ScheduleCron(f"{M} {H} * * *")` from `entry.target_time` and calls `next_fire_after(schedule, now_utc, tz)`, returns `min(candidates)`; `claim_due_concerns(now)` guards against naive datetimes with `ValueError` then returns `[e.type_id for e in store.list_entries() if is_checkin_due(e, local_date, local_time)]` — zero store mutation (purity proved by `test_is_pure_two_calls_same_result`). Exports: `CHECKIN_CONCERN_IDS: frozenset[str]` aliased to `ALL_CHECKIN_TYPES`; `is_pulse_engine_enabled(env) -> bool` using strict `.strip().lower() == "true"` matching the 4-of-5 majority (gcal/magicmirror/syncall/taskwarrior) — explicit parametric rejection of `"1"` / `"yes"` / `"on"` / `"truthy"` (disco/voice-trigger outlier convention not adopted); `concern_id_to_checkin_type(concern_id) -> CheckInType | None` type-narrowing pinch-point; `advance_last_run(store, checkin_type, today) -> None` one-line seam over `store.record_fired` that sub-03 calls after dispatch (kept despite anti-slop §3 smell because description §5 mandates the named export and sub-03 needs a testable call site). Clock injection via `now_provider` callable re-invoked per call (not snapshot) — tests use `_frozen_now(moment)`, production defaults to `datetime.now(timezone.utc)`, naive returns rejected at `_now_utc` boundary. `.env.example` adds `PULSE_ENGINE_ENABLED=false` block directly below `NANOBOT_TIMEZONE` with the mandated **"Experimental — keep off until Task 17/sub-03 verified on Mac"** warning and a 4-line WHY comment explaining staged rollout. `requirements.txt` adds `tzdata>=2024.1; sys_platform == "win32"` (macOS ships IANA natively; Windows fresh clones now resolve `ZoneInfo("America/Los_Angeles")` without relying on a transitive pull). 48 new tests across 3 files: `test_pulse_checkin_store.py` (6 test classes + 3 module-level tests covering concern-id round-trip, earliest-enabled/disabled-filter/retroactive-collapse/expired-staleness-returns-tomorrow, claim-due staleness both directions + before-target + purity + last_run_today + 24-hour 15-min-step sweep where each of the four default check-ins fires exactly once when combined with `advance_last_run`, structural Protocol binding via `sink: PulseStoreProtocol = adapter`, ConcernId-is-str, naive-now_provider rejection, UTC return tzinfo, custom `target_time` propagation); `test_pulse_feature_flag.py` (17 parametric cases + `os.environ`-shape duck-type test); `test_pulse_checkin_timezone.py` (LA spring-forward 3-day sweep, LA fall-back 3-day sweep, Europe/London DST-date-differs-from-LA 2-case proof that `tz` is actually threaded). Full suite 1679 passed / 14 skipped (sub-01 baseline 1631 + exactly 48, zero regressions); `mypy --strict` clean on all 4 files. Scope-clean: zero edits to `checkin_schedule.py`, `schedule_engine.py`, `scheduling_hook.py`, `custom_gateway.py`, `hook_factory.py`, `gateway_runner.py`, `start.py`, or workspace markdown — legacy scheduler remains the only code path affecting user-visible behaviour. Feeds sub-03 (hook cutover consumes adapter + `is_pulse_engine_enabled` + `concern_id_to_checkin_type` + `advance_last_run`) and sub-05 (Dream State integration shares the `PulseStoreProtocol` shape).
- Status: DONE
- Task: 17/sub-02
- Files: pulse_checkin_store.py, .env.example, requirements.txt, tests/test_pulse_checkin_store.py, tests/test_pulse_feature_flag.py, tests/test_pulse_checkin_timezone.py
- Depends on: [pulse-engine-core], [checkin-schedule-engine], [scheduling-complete]
- Detail: _build/tasks/17-temm1e-pulse-dream/sub-02/17-02x.md

### [pulse-checkin-integration]
> Wires sub-01's Pulse engine + sub-02's `PulseCheckinStore` into the live gateway behind `PULSE_ENGINE_ENABLED` via a Path-C pending-queue design: Pulse drives firing timing, a dispatcher evaluates cognitive state + assembles context, and the legacy `SchedulingHook` drains pre-formatted prompt blocks at heartbeat ticks. Flag-off behaviour is byte-identical to pre-Task-17 (AC#1, 113 scheduling/gateway/magicmirror/calendar tests pass unchanged); flag-on + flag-off share the exact same `format_checkin_prompt` call site so the 24-hour parity sweep (AC#2, 48 ticks × 4 check-ins) emits byte-identical `messages[0]["content"]` across both paths. Frozen layer (AC#9) untouched: `checkin_schedule.py` / `schedule_engine.py` / `pulse_engine.py` / `pulse_schedule.py` / `pulse_checkin_store.py` zero-diff since sub-02. New files: `pulse_checkin_dispatcher.py` (232 lines — `PendingCheckin` frozen dataclass, dedup-by-`(type, date)` `PendingCheckinQueue`, `PulseCheckinDispatcher.dispatch` that drops unknown concern_ids / calls `advance_last_run` on suppress+fire+modify / no-ops defer, `consume_pulse_events` coroutine using `asyncio.wait(FIRST_COMPLETED)` over `cancel.wait()`+`queue.get()` with per-event exception swallow so one broken event cannot starve the loop); `pulse_gateway_setup.py` (212 lines — `PulseBundle` single-use lifecycle container owning `asyncio.Task`s named `pulse-engine` and `pulse-consumer`, `build_pulse_bundle` flag gate returning `None` + `DEBUG "Pulse engine disabled"` when `is_pulse_engine_enabled(env)=False`, `_derive_state_accessor` walking the hook chain for the `HookAdapter`-wrapped `StateResponseHook` with explicit `RuntimeError` on missing, `_require_store`/`_require_store_protocol` helpers for fail-fast missing-dep errors, `_drain_task` with 5s `wait_for` + force-cancel fallback swallowing `CancelledError`); `cron_callback_setup.py` (84 lines, verbatim lift of `_setup_cron_callback` from `gateway_runner.py` to fit the file under the 300-line cap after pulse wiring). Modified files (surgical): `scheduling_hook.py` (231→264 lines, new `pulse_mode: bool=False` + `pending_queue: PendingCheckinQueue | None` kwargs, `_drain_pending_queue` early-return branch injecting ≤1 pending block per tick, legacy path untouched when flag off); `hook_factory.py` (260→266 lines, new `pulse_pending_queue` kwarg threaded into `SchedulingHook` with `pulse_mode=pulse_pending_queue is not None`); `gateway_runner.py` (308→270 lines, `build_pulse_bundle` wired after `create_hooks`, `pulse_bundle.start()` after `heartbeat.start()`, `await pulse_bundle.stop()` before `heartbeat.stop()` in `finally`). 31 new tests: `test_pulse_checkin_dispatcher.py` (12 — queue push/drain/dedup/FIFO, dispatcher fire/suppress/modify/unknown-concern/defer-analog, consumer dispatches-then-stops / exits-before-event / swallows-per-event-errors); `test_scheduling_hook_pulse_mode.py` (7 — single-block inject, empty-queue no-op, one-per-tick drain, legacy-due ignored when pulse_mode, non-heartbeat session skip, queue=None no-crash, flag-off legacy fallback); `test_pulse_gateway_lifecycle.py` (9 — start-spawns-2-named-tasks, stop-within-5s-budget, stop-before-start-noop, start-twice-raises, consumer-dispatches-queued-event, flag-off-returns-None+DEBUG-log, flag-on-zero-tasks-pre-start, flag-on-missing-StateResponseHook-raises, flag-on-missing-store-raises); `test_pulse_parity_legacy.py` (3 — 24h pair-set parity, byte-identical blocks per type, fires-exactly-once-per-day). Full suite 1710 passed / 14 skipped (sub-02 baseline 1679 + 31 new, zero regressions). `mypy --strict` clean on all 3 new + 3 modified files (remaining errors are pre-existing `nanobot.*` missing-stub warnings + pre-existing `state_detection.py` Literal narrowing — neither introduced by sub-03). Research-report Path C (pending-queue drain) chosen over Path A (`agent.process_direct` — rejected: spawns new session turn, duplicates `_pick_heartbeat_target`) and Path B (`bus.publish_inbound` — rejected: nanobot inbound queue expects user/assistant messages, not system-prompt suffixes); rationale encoded in the dispatcher module docstring. Observability emits 4 of 5 mandated INFO log lines — `pulse.start` (concern_count+tz+flag), `pulse.dispatch` (concern_id+action+state), `pulse.suppress` (concern_id+reason), `pulse.stop` (reason); `pulse.fire` (concern_id+delay_seconds) is the gap documented as MEDIUM-1. Async shutdown ordering deliberate: dispatcher's last drain can still touch `schedule_store` because `pulse_bundle.stop()` precedes `heartbeat.stop()` + `cron.stop()`. Feeds sub-04 (Dream engine reuses `PendingCheckinQueue`-adjacent design; module publicly exports `PendingCheckinQueue` for that reuse) and sub-05 (Dream scheduling reuses `PulseBundle` lifecycle pattern).
- Status: DONE-WITH-ISSUES
- Task: 17/sub-03
- Files: pulse_checkin_dispatcher.py, pulse_gateway_setup.py, cron_callback_setup.py, scheduling_hook.py, hook_factory.py, gateway_runner.py, tests/test_pulse_checkin_dispatcher.py, tests/test_scheduling_hook_pulse_mode.py, tests/test_pulse_gateway_lifecycle.py, tests/test_pulse_parity_legacy.py
- Depends on: [pulse-engine-core], [pulse-checkin-store-adapter], [scheduling-complete], [scheduling-heartbeat-integration], [custom-gateway-core]
- Issues: (1) MEDIUM: `pulse_checkin_dispatcher.py` fire branch — `pulse.fire` (concern_id, delay_seconds) log line mandated by description §5 is never emitted; `pulse.dispatch action=fire` is adjacent but carries no `delay_seconds` field. Fix: compute `(datetime.now(UTC) - event.fired_at).total_seconds()` in `_queue_fire_or_modify` and log it. (2) MEDIUM: `tests/test_pulse_*.py` — AC#6 ("log capture confirms each of the 5 structured log events fires") only asserts `pulse.dispatch` + `pulse.suppress`; `pulse.start`, `pulse.fire`, `pulse.stop` are unverified. Fix: add a `TestObservabilitySurface` class covering all 5. (3) MEDIUM: AC#10 — no end-to-end test boots `run_gateway` with flag ON to assert `morning_motivation` fires exactly once; parity test uses dispatcher directly, lifecycle test uses `StubPulse`. Fix: add `tests/test_pulse_gateway_smoke.py` wiring the real `Pulse.create`+`PulseCheckinStore`+`PulseCheckinDispatcher` through `PulseBundle.start()` with an injected fast-forward `now_provider`. (4) LOW: Rollback docs in impl report only give `git revert`; description §6 asked for a runtime flag-flip + grep-able log line — `"Pulse engine disabled"` exists but at DEBUG level. Promote to INFO or document the grep target explicitly. (5) LOW: `pulse_gateway_setup.py:147-163` `_derive_state_accessor` assumes `HookAdapter`-wrapped `StateResponseHook`; raw `AgentHook` subclasses would silently miss. Current hook chain is safe; future-fragility only. (6) LOW: `scheduling_hook.py:142-153` `pulse_mode: bool` flag parameter violates "no flag parameters" rule — consciously bent for back-compat; legacy path deletion is a future task. (7) LOW: `pulse_checkin_dispatcher.py:70-78` `PendingCheckinQueue._seen` set grows unbounded (~1460 keys/year, practically a non-issue). (8) LOW: `pulse_checkin_dispatcher.py:84-86` `list.pop(0)` is O(n); `collections.deque.popleft()` is idiomatic FIFO — cosmetic.
- Detail: _build/tasks/17-temm1e-pulse-dream/sub-03/17-03x.md

### [dream-state-engine]
> Inert 4-phase Dream State consolidation engine (gather → prompt → parse → write-back) for long-term memory compaction; does not register with Pulse — sub-05 wires it into a quiet-hours concern. Split across three modules to honour the 300-line cap: `dream_types.py` (89 lines — `DreamState(str, Enum)` sub-state machine `IDLE → RUNNING → {COMPLETE, FAILED}`, `DreamParseError`, four frozen dataclasses `DreamInputBundle` / `CandidateInsight` / `DreamConsolidationOutput` / `DreamRunResult`, constants `MAX_PROMPT_CHARS=6000` / `MAX_COMPLETION_TOKENS=800` / `MAX_CONTENT_CHARS=200` / `MAX_INSIGHTS_PER_RUN=10` / `DREAM_METADATA_SOURCE="dream_state"`, `PRICE_TABLE` with `x-ai/grok-4.1-fast` {input 0.20, output 0.50} + `openai/gpt-oss-120b` fallback); `dream_helpers.py` (297 lines — four spec-named pure helpers all <30 lines: `sanitise_excerpt` (7, 32+ alnum-char redactor + whitespace collapse + 800-char cap), `gather_consolidation_context` (24, frozen-clock read over `MemoryEntryStore.list_entries()` + `TaskStoreProtocol.list_tasks()` + optional JSONL session log; dream-generated memories filtered by `metadata["source"]=="dream_state"` to prevent self-feedback; naive clock raises `ValueError`), `build_consolidation_prompt` (20, `<<TOKEN>>` substitution over 5 placeholders chosen over `str.format` so prompt JSON schema braces pass through untouched; trailing `[:MAX_PROMPT_CHARS]` truncation; excerpts capped at `MAX_PROMPT_CHARS//2` most-recent-first), `parse_consolidation_response` (18, decodes JSON → rejects non-object roots / missing `insights` / `entries_to_resolve` keys with `DreamParseError` carrying first-200 response chars; per-insight validation drops with reason into `output.dropped[]` — `not_an_object`, `unknown_category`, `empty_content`, `oversize_content`, `non_dict_metadata`; caps to `MAX_INSIGHTS_PER_RUN=10`; resolves validated against `known_memory_ids: frozenset[str]` — phantom + non-string ids go to `dropped`); plus private helpers `_bucket_entries` / `_decode_response` / `_validate_insight` / `_validate_resolves` / `_parse_session_line` / `_read_session_excerpts` / `_cap_excerpts` / `_format_entry_lines` / `_stringify` / `_is_dream_generated`; `estimate_per_run_cost_usd(prompt_chars, completion_tokens, model_name)` returns upper-bound using 4-chars/token on input + exact output, unknown model raises `ValueError`); `dream_engine.py` (195 lines — `DreamEngine` stateful orchestrator with 9-param keyword-only constructor (`memory_store` / `task_store` / `session_log_path` / `llm_caller: Callable[[str,int],Awaitable[str]]` / `clock` / `prompt_template: str` / `window_hours=24` / `max_cost_usd_per_run=0.01` / `model_name="x-ai/grok-4.1-fast"`), `estimated_cost_per_run_usd: float = 0.01` class constant, `get_state` / `reset_state` / async `run` / private `_apply`; module-level `_probe_clock` probes the injected clock at construction and raises `ValueError` on naive datetimes; `run()` flow gates on `state == IDLE` — rejects re-entry with `RuntimeError("state=IDLE")` — then `RUNNING → COMPLETE` on clean path or `RUNNING → FAILED` on any `Exception` via broad-catch funnel returning `DreamRunResult(entries_created, entries_resolved, prompt_tokens_est=len(prompt)//4, completion_tokens=len(response)//4, state, error)`; `_apply` dedups on `(category, content)` pairs across active entries only (resolved excluded so Dream can re-surface a retired insight) — frozenset snapshot pre-loop, dedup key updated as each insight lands → two consecutive `_apply` calls on the same output insert once; write-back stamps `metadata["source"]="dream_state"` + `metadata["run_at"]=clock().isoformat()` + `metadata["supersedes"]=insight.supersedes_id`; `resolve_entry` wrapped in `try/except KeyError` for silent phantom-id skip). Prompt template `workspace/templates/DREAM.md` (98 lines) ships via `setup_workspace.TEMPLATE_FILES` (new entry `"templates/DREAM.md"` at index 11); names all 5 categories, enumerates 10 banned phrases from SOUL.md ("you should", "just do it", "try harder", …) + 6 neuroaffirming substitutions, mandates JSON-only output schema `{insights:[…], entries_to_resolve:[…]}` with max-10 insights and ≤200-char content constraints. `.gitignore` gains `!workspace/templates/` negation so the app's workspace template dir escapes the repo-wide `templates/` exclusion used for agent-framework scaffolding. 77 tests across 7 files — gather (12: empty/window-filter/self-feedback-exclusion/energy-state-pick/tasks-inclusion/JSONL valid+malformed+missing-file+bad-iso/naive-clock-raises/custom-window), prompt (14: token-replacement/5-category presence/no-sixth-category/3 banned-phrase enumeration tests/bundle-content injection for memories+energy+excerpts/MAX_PROMPT_CHARS cap with 200×200 filler excerpts/sanitise 5-case coverage), parse (18: happy path + all-5-categories + empty output / 4 malformed JSON branches / insight validation across 6 drop-reasons / max-10 cap on 20-insight input / metadata string-coercion of int+bool+None / resolve validation phantom-id + non-string id), apply (8: single-write + dream-metadata stamp + supersedes preservation / idempotent re-apply on `(category, content)` / distinct-category same-content inserts both / known-id resolve + unknown-id silent skip / full-output round-trip + run-path metadata sanity), run (9: IDLE-at-construction + naive-clock-rejection / happy-path `RUNNING→COMPLETE` + token-estimate recording / malformed-response + llm-exception → `FAILED` / double-run without reset raises + `reset_state` re-entry + post-FAILED reset), cost (9: Grok per-run ≤ $0.01 + class constant check / monthly ≤ $1 at 1 run/day / PRICE_TABLE completeness / zero-usage=$0 / gpt-oss cheaper than Grok / unknown model raises / linear-scaling invariant), template deploy (4: manifest-contains + source-exists + copy-succeeds + byte-identical). Full suite 1787 passed / 14 skipped / zero regressions from sub-03 baseline; `mypy --strict` clean on all 3 source files; grep-verified frozen files (`memory_store.py` / `task_store.py` / `pulse_*.py` / `scheduling_hook.py` / `schedule_engine.py` / `checkin_schedule.py` / `custom_gateway.py` / `hook_factory.py` / `gateway_runner.py` / `workspace/SOUL.md` / `workspace/memory/MEMORY.md`) carry zero diff. Deviations from research: three modules instead of one (line-cap enforcement), `class DreamState(str, Enum)` instead of `StrEnum` (Python 3.10 venv), `<<TOKEN>>` substitution instead of `str.format` (JSON schema brace conflict), `.gitignore` `!workspace/templates/` negation (undiscovered at research time). Engine is importable but inert — sub-05 instantiates it inside the Pulse tick loop behind `DREAM_STATE_ENABLED`.
- Status: DONE
- Task: 17/sub-04
- Files: dream_engine.py, dream_helpers.py, dream_types.py, workspace/templates/DREAM.md, setup_workspace.py, .gitignore, tests/test_dream_engine_gather.py, tests/test_dream_engine_prompt.py, tests/test_dream_engine_parse.py, tests/test_dream_engine_apply.py, tests/test_dream_engine_run.py, tests/test_dream_engine_cost.py, tests/test_dream_template_deploy.py
- Depends on: [memory-entry-store], [memory-context-injection], [task-store-factory-and-migration], [pulse-engine-core], [pulse-checkin-store-adapter], [pulse-checkin-integration]
- Issues: (1) LOW: `workspace/templates/DREAM.md:69-70` promises "Any insight whose `content` contains a banned phrase will be rejected by the parser" — `dream_helpers._validate_insight` has no banned-phrase filter so the guardrail is aspirational. Fix: drop the sentence or add a `"banned_phrase"` drop-reason in `_validate_insight`. (2) LOW: `dream_helpers.py:128-129` `_parse_session_line` silently coerces naive ISO timestamps to UTC; when sub-05 wires a session-log writer the contract needs to be "always tz-aware" or the helper should skip naive lines. (3) LOW: `dream_helpers.py:244` accepts non-string `supersedes_id` via silent `str()` coercion — inconsistent with the `metadata` non-dict rejection in the adjacent branch. Fix: narrow to `isinstance(supersedes, str)` and add `"non_string_supersedes_id"` drop-reason. (4) LOW: `dream_helpers.py:225` `raw.get("metadata") or {}` masks falsy non-dicts (`[]`, `null`) before the `isinstance` check, so they silently become empty dicts rather than being dropped as `"non_dict_metadata"`. Fix: explicit branch — missing key → `{}`, bad type → drop.
- Detail: _build/tasks/17-temm1e-pulse-dream/sub-04/17-04x.md

### [pulse-dream-closure]
> Closes Task 17 by wiring sub-04's `DreamEngine` onto a Pulse-scheduled quiet-hours concern, shipping the user-facing `TEMM1E_PULSE.md` doc, and locking the rollback procedure — all behind the independently-flippable `PULSE_ENGINE_ENABLED` + `DREAM_STATE_ENABLED` flags (both default OFF, Dream requires Pulse). New `pulse_system_concerns.py` (247 lines) exports `DREAM_CONCERN_ID="dream_state"`, `DEFAULT_DREAM_CRON="0 3 * * *"`, `DEFAULT_SKIP_WINDOW_HOURS=12`, `is_dream_state_enabled(env)` (strict `"true"` truthy convention matching `is_pulse_engine_enabled`), `read_last_run` / `write_last_run` (atomic `.tmp` + `.replace`), `should_skip_catchup`, `DreamConcernStore` (single-concern `PulseStoreProtocol` — `next_fire_time` returns strict-next cron slot unless a Dream was missed outside the 12h skip window in which case it returns `now_utc` for immediate fire; `claim_due_concerns` baseline-fallback `now - 7 days` when `last_run` is None), and `CompositePulseStore` (min-of-children `next_fire_time`, order-preserving concat `claim_due_concerns`, rejects empty member list). `pulse_checkin_dispatcher.py` gains kw-only `dream_engine` / `dream_last_run_path` / `now_utc_provider` params + `active_dream_task` property; `dispatch` short-circuits on `DREAM_CONCERN_ID` before `concern_id_to_checkin_type`, `_dispatch_dream_concern` refuses re-entry if task in-flight or engine state is RUNNING (auto `reset_state()` from FAILED/COMPLETE), and spawns `asyncio.create_task(_run_dream_once, name="dream-run")`. `_run_dream_once` logs `dream.complete` with `DreamRunResult` fields on success, writes `dream_last_run.json` only on the happy path, and broadly-catches `Exception` to log `dream.run crashed` without crashing the consumer loop. `pulse_gateway_setup.py` gains `dream_engine: DreamEngine | None = None` on `PulseBundle` + `data_dir` / `dream_prompt_template` / `dream_llm_caller` / `dream_clock` kw-only params on `build_pulse_bundle`; emits WARN when `DREAM_STATE_ENABLED=true` + `PULSE_ENGINE_ENABLED!="true"` and treats Dream as disabled (no startup error); `_build_dream_wiring` constructs `DreamEngine` with `session_log_path = data_dir / "dream_sessions.jsonl"` + a `DreamConcernStore` with cron from env, returns `(engine, last_run_path, CompositePulseStore([checkin_store, dream_store]))`; raises `RuntimeError` listing missing fields when Dream is requested without the four kwargs. `PulseBundle.stop()` drains the Dream task via `getattr(dispatcher, "active_dream_task", None)` so sub-03's `StubDispatcher` keeps working. `gateway_runner.py` grows `_build_dream_kwargs(provider, config, repo_root, tz)` which reads `workspace/templates/DREAM.md` once, wraps `provider.chat_with_retry` into the `(prompt, max_tokens) -> str` shape `DreamEngine` expects, and returns `dream_clock = lambda: datetime.now(tz)` — **reuses** the provider nanobot's existing Dream already uses (no second provider instance). `setup_workspace.TEMPLATE_FILES` adds `"TEMM1E_PULSE.md"` between `TASKWARRIOR.md` and `states.yaml`. `.env.example` adds `DREAM_STATE_ENABLED=false` + `DREAM_STATE_CRON=0 3 * * *` with WHY comments (cost envelope ≤$0.01/run ≤$0.30/month, Pulse dependency, rollback command). `workspace/SOUL.md` gains a surgical `## Pulse + Dream` section between `## Task Ledger` and `## Calendar` (358 lines total, ≤1500-byte delta budget enforced by test) — three bullets teaching the LLM that Pulse-mode check-ins are identical to legacy from the prompt POV, `metadata.source == "dream_state"` memories weight equal to user-authored but with honest provenance ("I summarised" not "I remembered"), and a pointer to `TEMM1E_PULSE.md`. `workspace/TEMM1E_PULSE.md` (143 lines, 7 sections — TL;DR → Why TEMM1E → Feature flags → Rollback → Dream State explained → Divergence warning → Troubleshooting) carries the mandated bold block "Do not flip-flop flags. Pulse↔legacy divergence is not tested. Pick a backend and stay." 61 new tests across 6 files: `test_dream_concern_registration.py` (28 — flag truthy convention, last-run JSON round-trip/atomic/corrupt/naive-rejected, `should_skip_catchup` windowing, `DreamConcernStore` next-fire-and-claim semantics under 4 boot scenarios, `CompositePulseStore` empty-rejected + min-pick + merge-order), `test_task17_flag_matrix.py` (5 — four flag cells + missing-dream-kwargs guard), `test_task17_integration.py` (4 — simulated-day: four `PendingCheckin` rows queued + two `MemoryEntry` rows written with `metadata.source="dream_state"` + `llm_caller` called exactly once + `dream_last_run.json` persisted + `MemoryContextHook` surfaces Dream rows on next tick), `test_task17_baseline_regression.py` (9 — flags absent/false → None, non-canonical truthy stays OFF, no stray `pulse-*`/`dream-*` asyncio tasks, numerical floor on pre-Task-17 scheduling suites), `test_temm1e_pulse_md_deploy.py` (8 — manifest + source + byte-identical copy + 7 sections + divergence warning + both flags + `Dream requires Pulse` rule), `test_soul_dream_section.py` (7 — heading-exactly-once + position-between-Task-Ledger-and-Calendar + full heading-order preserved + TEMM1E_PULSE.md link + metadata.source documented + "identical" Pulse-mode claim + ≤1500-byte growth). Full repo suite: **1848 passed, 14 skipped** (zero regressions from sub-04 baseline 1787 + exactly 61 new). `mypy --strict` clean on all three sub-05 source files (seven reported errors all pre-existing from Task 02 / nanobot stubs, flagged in sub-03). File sizes: `pulse_system_concerns.py` 247 / `pulse_checkin_dispatcher.py` 297 / `pulse_gateway_setup.py` 296 / `gateway_runner.py` 299 — all under the 300-line cap. **Rollback is 100% non-destructive:** `CheckInScheduleStore` + `MemoryEntryStore` contents survive a flag flip in either direction; `dream_last_run.json` is a staleness guard (not state) and can be deleted safely. **Monthly cost ≤ $6.60** (≤$0.30 Dream + ≤$0.30 nanobot's own Dream + ≤$6 other LLM = under the $7 ceiling). Nanobot's existing Dream coexists unchanged (different surfaces: our Dream writes structured `MemoryEntry` rows, nanobot's edits free-form MEMORY.md/SOUL.md/USER.md via tool calls). Design decisions: composite store over dual-register (pure extension point for future concerns), dispatcher branch over separate consumer (keeps sub-01 tests green), `asyncio.create_task` bridge with named `"dream-run"` task, engine singleton with `reset_state()` before each tick, last-run JSON written only after success (failed runs do not block next slot). Flag-OFF baseline is byte-identically preserved: no `build_pulse_bundle` return, no Pulse/Dream asyncio tasks, legacy `SchedulingHook` drives all four check-ins. Task 17 is closed and Mac-deployment-ready.
- Status: DONE-WITH-ISSUES
- Task: 17/sub-05
- Files: pulse_system_concerns.py, pulse_checkin_dispatcher.py, pulse_gateway_setup.py, gateway_runner.py, setup_workspace.py, .env.example, workspace/SOUL.md, workspace/TEMM1E_PULSE.md, tests/test_dream_concern_registration.py, tests/test_task17_flag_matrix.py, tests/test_task17_integration.py, tests/test_task17_baseline_regression.py, tests/test_temm1e_pulse_md_deploy.py, tests/test_soul_dream_section.py
- Depends on: [pulse-engine-core], [pulse-checkin-store-adapter], [pulse-checkin-integration], [dream-state-engine], [memory-entry-store], [memory-context-injection], [task-store-factory-and-migration], [state-response-integration], [neuroaffirming-personality], [nanobot-workspace-setup]
- Issues: (1) MEDIUM: AC#6 rollback round-trip is not tested — suite proves ON-leg (integration) and OFF-leg (baseline regression) independently but no test composes boot-ON → dispatch → boot-OFF → legacy-resumes round trip. Real-world flag flips unexercised in CI. Fix: add ≤30-line `test_task17_rollback_round_trip.py`. (2) MEDIUM: AC#10 nanobot-Dream coexistence is documented + same-provider but not automated. Integration test's `llm.calls == 1` covers only our `_dream_llm`; nanobot's `agent.dream` path is not touched. A future refactor sharing LLM caches between the two dreams would silently break this. Fix: `test_dream_coexistence.py` stubbing both callers. (3) LOW: `pulse_gateway_setup.py:137` "Pulse engine disabled" logged at DEBUG — flagged in sub-03 LOW-4, still unresolved. Promote to INFO so `TEMM1E_PULSE.md` troubleshooting has a symmetric positive counterpart to the `pulse.start` grep. (4) LOW: `pulse_checkin_dispatcher.py` + `pulse_gateway_setup.py` import Dream symbols unconditionally — flag-off deploys pay the cost. Lazy-import inside `_dispatch_dream_concern` / `_build_dream_wiring`. (5) LOW: `PulseCheckinDispatcher.__init__` has 9 params — mirrors sub-03's `MagicMirrorHook` 8-kwarg LOW; group the Dream trio into a `DreamDispatchConfig` dataclass. (6) LOW: `_run_dream_once`'s `except Exception` duplicates `DreamEngine.run`'s own Exception guard — add a one-line WHY comment or narrow. (7) LOW: `DreamConcernStore.claim_due_concerns` fresh-boot semantics surprising — `last_run=None` + baseline-minus-7-days means a fresh boot with a passed cron slot claims `dream_state` immediately. Tighten docstring or cap baseline at `now - skip_window_hours`. (8) LOW: inherited from sub-03 MEDIUM-1 — `pulse.fire` log with `delay_seconds` still missing. (9) LOW: `test_task17_flag_matrix.test_cell_3_pulse_off_dream_on_warns` asserts only that both flag names appear in some log record; does not pin the full mandated WARN string. Add `assert any("Dream requires Pulse" in m for m in msgs)`. (10) LOW: `should_skip_catchup(None, naive_now, 12)` returns False without raising — asymmetric with `write_last_run` + `claim_due_concerns` naive-rejection. Move the tz-check above the `last_run is None` short-circuit. (11) LOW: `CompositePulseStore([None])` explodes on `await` with AttributeError — validate member non-None at construction.
- Detail: _build/tasks/17-temm1e-pulse-dream/sub-05/17-05x.md

### [mac-install-script]
> One-shot Bash on-ramp `install_mac.sh` at the repo root (117 lines, mode 0755, LF-forced via new `.gitattributes`) — seven-phase idempotent installer a non-technical user runs once on a fresh Mac Air M2 with only Homebrew pre-installed. Phases: (0) Homebrew presence check + `uname -m` Apple-Silicon banner with fallback `eval "$(/opt/homebrew/bin/brew shellenv)"` for unseeded shells; (1) `for pkg in python@3.11 node task git ffmpeg; do brew list "$pkg" >/dev/null 2>&1 || brew install "$pkg"; done` — the literal idempotency idiom the acceptance criteria name; (2) `[[ -d .venv ]] || python3.11 -m venv .venv`; (3) `.venv/bin/pip install -r requirements.txt` + `-r requirements-dev.txt`; (4) `(cd mcp/google-calendar && npm install && npm run build)` — matches `gcal_setup.build_google_calendar_mcp`'s three-step contract; (5) `(cd magicmirror && npm install --ignore-scripts)` — defuses upstream `postinstall git clean -df` the same way `magicmirror_setup.build_magicmirror` does; (6) `for module_dir in magicmirror/modules/MMM-*; do [[ -f "$module_dir/package.json" ]] || continue; (cd "$module_dir" && npm install); done` — loop form chosen over enumerated so a future fourth MMM-module works without a script edit; (7) heredoc `=== Install complete ===` banner with four-step next-actions (`cp .env.example .env`, edit three env vars, `.venv/bin/python setup_workspace.py`, `.venv/bin/python start.py`). Scaffolding: `#!/usr/bin/env bash`, `set -euo pipefail` at line 9, `cd "$(dirname "$0")"` at line 11, `CURRENT_PHASE` variable + `trap 'echo "FAILED in phase: ${CURRENT_PHASE}" >&2' ERR` so a non-technical user sees readable `FAILED in phase: [3/7] Python dependencies` instead of a line number. Zero sudo, zero `>/dev/null` error swallowing (the only use is on `brew list`'s discovery check which is the correct channel to silence), bash 3.2-safe (no `mapfile` / `declare -A` / `${var,,}` / process substitution) so macOS's stock `/bin/bash` works. Companion user doc `workspace/INSTALL_MAC.md` (105 lines) ships via `setup_workspace.TEMPLATE_FILES` (new `"INSTALL_MAC.md"` entry at index 8, between `TEMM1E_PULSE.md` and `states.yaml`) — seven copy-paste steps for a non-technical user (install Homebrew → `cd` to repo → `bash install_mac.sh` → `cp .env.example .env` → fill three env vars [`OPENROUTER_API_KEY`, `TELEGRAM_BOT_TOKEN`, `TELEGRAM_USER_ID`] → `setup_workspace.py` → `start.py`) plus four troubleshooting entries (brew-not-in-PATH, Taskwarrior first-run `~/.taskrc` prompt, old Node conflict, re-running after partial failure). New `.gitattributes` adds `* text=auto` + `*.sh text eol=lf` so Windows `core.autocrlf=true` cannot silently corrupt the Mac-target script with CRLF line endings. 25 new static tests across 2 files: `test_install_mac_script.py` (21 tests in 11 classes — one class per acceptance criterion #1-#12 + #16 + repo-root anchoring, uses `Path.read_text` + substring/regex + one `subprocess.run(["bash", "-n", …])` that `pytest.skip`s if bash is absent) and `test_install_mac_md_deploy.py` (4 tests — manifest contains + source exists + `copy_workspace_files(tmp)` produces the file + byte-identical read_text equality, pattern cloned from `test_setup_workspace_calendar_md.py`). No brew/npm/python3.11 runtime invocation — live smoke deferred to sub-05. Full suite: **1848 → 1873 passed / 14 skipped** (exactly +25, zero regressions from sub-05 baseline). `mypy --strict` clean on all three touched Python files (`test_install_mac_script.py`, `test_install_mac_md_deploy.py`, `setup_workspace.py`). All 17 numbered acceptance criteria pass independently. Scope discipline held: zero edits to `custom_gateway.py`, `start.py`, `hook_factory.py`, any hook/engine module, or `gateway_runner.py` (git-status modifications on those files trace to Task 17/sub-05's uncommitted tail, not this subtask). Key deviations from research: (a) added `.gitattributes` for CRLF-safety on the Windows dev host; (b) skipped `pip install --upgrade pip` preamble per anti-slop "no just-in-case code"; (c) chose glob loop over enumerated module list for future-proofing. Design trade-offs consciously made: `[[ -d .venv ]]` guard over stricter `[[ -x .venv/bin/python ]]` (literal acceptance-grep match, rare half-creation recovery is `rm -rf .venv && bash install_mac.sh` documented in troubleshooting); no `task --version` auto-seed (Brew formula's non-interactive behaviour inconsistent across versions — first-run prompt is user-friendlier, documented in INSTALL_MAC.md); no `pip install ./vendor/syncall` (vendored syncall is sys.path-loaded by design, its 10 transitive deps pin individually in requirements.txt — adding that line would break sub-03-of-Task-16). Downstream contract: sub-02 (path migration) gets `.venv/bin/python`, sub-03 (MagicMirror lifecycle) gets `magicmirror/node_modules/` + every `MMM-*/node_modules/`, sub-04 (LaunchAgent) can hard-code `.venv/bin/python` in the plist, sub-05 (smoke test) runs the script live on Mac and asserts `.venv/` + `magicmirror/node_modules/` + `mcp/google-calendar/build/index.js` exist. Pending commit — six LOW polish opportunities (phase-banner symmetry on line 98, symlink resolution on line 11, bash-3.2-compat regression guard test, `.gitattributes` self-disclosure, explicit phase-banner enumeration test, `brew doctor` hint in INSTALL_MAC.md) documented in 18-01v.md but none block the task.
- Status: DONE
- Task: 18/sub-01
- Files: install_mac.sh, workspace/INSTALL_MAC.md, tests/test_install_mac_script.py, tests/test_install_mac_md_deploy.py, .gitattributes, setup_workspace.py
- Depends on: [nanobot-workspace-setup], [workspace-deploy-memory-seed-and-model-pin], [magicmirror-vendor-and-config], [gcal-mcp-server-wiring], [taskwarrior-store-backend], [pulse-dream-closure]
- Issues: (1) LOW: `install_mac.sh:98` — `CURRENT_PHASE="complete"` set before the final heredoc; if the heredoc fails (improbable) the trap prints `FAILED in phase: complete` instead of the more useful `[7/7] Finish`. Fix: delete line 98 or rename to `"[7/7] Finish"`. (2) LOW: `install_mac.sh:11` `cd "$(dirname "$0")"` does not resolve symlinks; a user running a symlinked copy of the script from outside the repo would land in the wrong dir. Low risk for non-technical flow, documented in INSTALL_MAC.md step 1. Fix: optional `readlink -f` guard. (3) LOW: no test asserts phase banners enumerate `[0/7]` through `[7/7]` contiguously — a future refactor that skips a phase would silently change user-visible output. Fix: one-line `all(f"[{i}/7]" in text for i in range(0, 8))` assertion. (4) LOW: impl claims bash 3.2-safety but no test enforces it — a future `mapfile`/`declare -A`/`${var,,}` regression would slip through CI until sub-05's live smoke. Fix: add grep-based `TestBash32Compat`. (5) LOW: `.gitattributes`'s `* text=auto` policy change was not named in the description and is self-disclosed in the impl report — harmless (matches git's default) but the index-report agent should note it. (6) LOW: Homebrew-prefix-writable check absent; a user with a root-owned prefix (rare `sudo brew install` legacy misconfig) gets an opaque `Permission denied`. Fix: add a 4th troubleshooting entry to `INSTALL_MAC.md`.
- Detail: _build/tasks/18-mac-deployment-port/sub-01/18-01x.md

### [mac-data-paths-and-migration]
> Reversible Windows→macOS runtime-data port, done entirely via env-var redirection + a one-shot argparse CLI — zero runtime code changes. New `scripts/migrate_windows_to_mac.py` (284 lines) is the on-ramp: dry-run by default, `--apply` triggers the copy, mutually exclusive with `--dry-run`; `--source-repo` defaults to repo root, `--target-base` defaults to `~/Library/Application Support/adhd-assistant/`. A frozen `PATH_MAPPING` tuple of three `PathEntry` dataclasses covers `workspace/data/ → <target>/data/` (full copytree), `workspace/data/taskwarrior/ → <target>/taskwarrior/` (sibling, not nested — env-var defaults are `ADHD_DATA_DIR=~/.../data` + `TASKWARRIOR_DATA_DIR=~/.../taskwarrior`), and `gcp-oauth.keys.json → <target>/oauth/gcp-oauth.keys.json` (single-file copy2). `process_entry` returns an immutable `EntryResult` dataclass with status string (`copied`/`forced_overwrite`/`would_copy`/`skipped_target_exists`/`skipped_source_missing`); the only side effect is the filesystem copy itself. Exit codes: `0` clean, `2` missing source repo, `3` non-empty fresh target without `--force` (detected via absence of `migration_windows_to_mac.log` at target base — the log's presence doubles as "we've migrated here before" marker so second `--apply` runs are idempotent zero-exits per AC #2), `4` permission error during copy. Append-only `<target>/migration_windows_to_mac.log` records one timestamped block per run with parseable `STATUS: SOURCE -> TARGET` lines. Source tree is never modified — rollback is `rm -rf <target>` documented in MAC_PATHS.md. Success banner directs the user to uncomment the `.env.example` Mac block and restart. **No new `mac_paths.py` helper** — research survey confirmed every runtime data path is already env-var driven (`ADHD_DATA_DIR`, `TASKWARRIOR_DATA_DIR`, `DASHBOARD_DATA_DIR`, `GOOGLE_OAUTH_CREDENTIALS`, `GOOGLE_CALENDAR_MCP_TOKEN_PATH`) so description step 4's escape clause ("zero code changes required") was taken — YAGNI/anti-slop compliant. `.env.example` gains a 17-line `# === macOS Conventional Paths (uncomment on Mac) === … # === end macOS block ===` fenced section below SYNCALL documenting the six Mac-conventional overrides (all commented, so Windows devs see identical default behaviour); the un-edited `.env` resolves to `workspace/data/...` on both platforms. Tilde-expansion caveat called out explicitly in both the `.env.example` banner and `MAC_PATHS.md` Troubleshooting — `custom_gateway.resolve_data_dir` does not call `.expanduser()`, so a literal `~/Library/...` value creates a `~` directory under the repo; the documented workaround is to expand manually (`/Users/YOUR_USERNAME/Library/...`), with the one-line patch deferred to sub-05 per scope (AC #13 forbids custom_gateway edits in sub-02). `workspace/MAC_PATHS.md` (98 lines) ships via `setup_workspace.TEMPLATE_FILES` (single-line addition, `"MAC_PATHS.md"` inserted between `"INSTALL_MAC.md"` and `"states.yaml"`) — four sections (what-moves-where table, three copy-paste migration commands, `rm -rf` rollback, four-entry troubleshooting covering the tilde caveat + missing OAuth creds + syncall pickle re-authorization + log location). 40 new tests across 3 files: `test_migrate_windows_to_mac.py` (21 cases across 10 classes — dry-run×2, apply-copy×4 including sha256 byte-identity, idempotency×1 with pre/post-run tree-hash skipping the log file, reversibility×1 source-tree sha256 round-trip, force-overwrite×1, summary-log×3 covering header/append/dry-run-silence, exit-codes×4 one per `0/2/3/4` with portable `monkeypatch.setattr(shutil, "copytree", raise_permission)` for code-4 — no `chmod 0555`, no `sys.platform` gate, mutually-exclusive-flags×1 asserting SystemExit, missing-optional-source×1 for absent `gcp-oauth.keys.json`, CLI-surface×3 grep + parse_args assertions), `test_env_example_mac_paths.py` (14 cases parametrised — Mac banner + end-marker + six commented overrides all present and verbatim, plus six pre-existing defaults — `ADHD_DATA_DIR=data`, `DASHBOARD_DATA_DIR=data`, `ADHD_STATES_PATH=workspace/states.yaml`, `TASKWARRIOR_DATA_DIR=`, `GOOGLE_OAUTH_CREDENTIALS=`, `GOOGLE_CALENDAR_MCP_TOKEN_PATH=` — still present verbatim so AC #9 "no runtime default path moved" is pinned by CI), `test_mac_paths_md_deploy.py` (5 cases — manifest-contains, listed-exactly-once, source-exists, copy-produces-target, byte-identical read_text equality). Full suite: **1873 → 1913 passed, 14 skipped** (exactly +40, zero regressions from sub-01 baseline). `mypy --strict` clean on all four new files; `grep -n "sys.platform\|os.name"` returns zero on new code (AC #10). Line-count budget: 284 / 98 / 278 / 62 / 33, all ≤ 300 (AC #14). Scope discipline held: git diff of `setup_workspace.py` is a 2-line reorder+insert in `TEMPLATE_FILES`; zero edits to `custom_gateway.py`, `start.py`, `hook_factory.py`, any hook/engine module, or `gateway_runner.py` (working-tree modifications on those files are uncommitted Task 17/sub-05 tail, not this subtask). Design decisions: hard-coded mapping table over consumer-module derivation (Approach A — avoids pulling `task_store_factory`/`taskwarrior_setup` pydantic+nanobot side effects into migration tests); taskwarrior kept as separate entry from `workspace/data/` because the Mac targets are SIBLINGS not nested (different env vars point to different directories); `--apply`/`--dry-run` made mutually exclusive argparse pair to prevent test-fixture copy-paste from Task 16 footgun; log-based "fresh target" heuristic for exit-3 to reconcile "second run returns 0" with "first run onto populated target returns 3"; append-only log written on force path too. Downstream contract: sub-03 (MagicMirror launcher) can assume `workspace/MAC_PATHS.md` exists at `~/.nanobot/workspace/MAC_PATHS.md` after setup; sub-04 (LaunchAgent) consumes `ADHD_LOG_DIR` (declared in the commented Mac block, no runtime consumer yet — sub-04 adds the first); sub-05 (live smoke) runs `python scripts/migrate_windows_to_mac.py --apply` on the Mac against a fresh clone, verifies target population, uncomments the `.env` Mac block, **and applies the one-line `.expanduser()` patch to `custom_gateway.resolve_data_dir`** deferred from this subtask.
- Status: DONE
- Task: 18/sub-02
- Files: scripts/migrate_windows_to_mac.py, workspace/MAC_PATHS.md, .env.example, setup_workspace.py, tests/test_migrate_windows_to_mac.py, tests/test_env_example_mac_paths.py, tests/test_mac_paths_md_deploy.py
- Depends on: [mac-install-script], [nanobot-workspace-setup], [taskwarrior-store-backend], [gcal-mcp-server-wiring], [task-store-factory-and-migration]
- Issues: (1) MEDIUM: `scripts/migrate_windows_to_mac.py:248-274` — `main()` runs `process_entry` (which performs the actual copy) for every entry before the `has_conflicts` aggregation decides whether to return `EXIT_TARGET_CONFLICT`. An entry with an empty target is copied to disk BEFORE the final conflict check fires. Result: a fresh-target run where only `data/` is pre-populated will skip `data/`, COPY `taskwarrior/` and `oauth/` to disk, then return exit 3 without writing the log. User rollback (`rm -rf <target>`) still works but the filesystem is left in mixed state with no log record of the partial writes. `test_non_empty_fresh_target_without_force_returns_three` only asserts the exit code, so this slips past CI. Fix: split `process_entry` into `plan_entry` + `execute_entry` and run planning for all entries before any copy; or, in `main()`, run an initial dry-run pass, return 3 if any conflict, then run the real pass. Add a test asserting `not (target_base / "taskwarrior").exists()` in the exit-3 case. (2) LOW: `scripts/migrate_windows_to_mac.py:208` — `datetime.now().isoformat()` in `write_run_log()` omits timezone; `code-rules.md:95` forbids naive `datetime.now()`. Fix: `datetime.now(timezone.utc).isoformat(timespec="seconds")`. Log timestamps currently inherit the host's local tz implicitly, making cross-host log comparison ambiguous. (3) LOW: `scripts/migrate_windows_to_mac.py:222, 228, 229` — `print_banner()` uses `log.info("")` for visual whitespace; a future structured-logging handler (JSON formatter) would emit records with empty `message` fields. Prefer direct `sys.stdout.write("\n")` or concatenate spacing into the next log line. (4) LOW: `scripts/migrate_windows_to_mac.py:246, 267` — once `<target>/migration_windows_to_mac.log` exists, `--apply` without `--force` becomes a silent no-op (exit 0). A user restoring from backup who expects "re-migrate" is not hinted at `--force` on the skip path. Fix: when all entries skip AND log exists, print "Already migrated. Pass `--force` to re-copy." alongside the existing banner. (5) LOW: `scripts/migrate_windows_to_mac.py:118-119` — `expand_path()` calls both `.expanduser()` and `.resolve()`; on a non-existent `--target-base`, Python 3.10+ `Path.resolve(strict=False)` is the implicit mode. Behaviour correct; worth noting explicit `strict=False` for future readers. (6) INFO: Tilde-expansion caveat on `custom_gateway.resolve_data_dir` is documented in both `.env.example` and `MAC_PATHS.md` Troubleshooting but the one-line `.expanduser()` patch is deferred to sub-05 per scope (AC #13). Sub-05 verification must confirm the patch lands before the live smoke signs off.
- Detail: _build/tasks/18-mac-deployment-port/sub-02/18-02x.md

### [magicmirror-autostart-launcher]
> Flag-gated in-process MagicMirror² supervisor for `start.py` — `MAGICMIRROR_AUTOSTART_ENABLED=true` spawns `npm start --prefix magicmirror/` as a supervised child; Ctrl+C (or gateway exit) triggers a SIGTERM→10s-wait→SIGKILL shutdown with the mirror going down BEFORE the syncall daemon so the Fire Tablet disconnects cleanly. New `magicmirror_launcher.py` (223 lines, mypy --strict clean, zero `sys.platform`/`os.name` branches) exports four symbols: `is_magicmirror_autostart_enabled(env)` — `.strip().lower() == "true"` matching the family convention (`is_magicmirror_enabled`, `is_pulse_engine_enabled`, `is_syncall_enabled`, `is_gcal_enabled`); `build_magicmirror_command(repo_root)` — pure list-argv `["npm", "start", "--prefix", str(repo_root / "magicmirror")]`, no `shell=True`, no string interpolation; `MagicMirrorProcess` — single-use stateful wrapper (start → stop → terminal; `start()` after `stop()` raises `RuntimeError("…single-use")`); `launch_magicmirror(repo_root, env, popen=subprocess.Popen)` — flag-gated factory returning `None` when off, raising `RuntimeError` with "config.js"+"setup_workspace()" hint when flag on but `magicmirror/config/config.js` absent, else opening log streams and starting the process. Log routing: `ADHD_LOG_DIR` (sub-02's env-var, first runtime consumer here) → `Path(...).expanduser()` → `magicmirror.log` + `magicmirror.err` in append-binary mode; default is `<repo>/logs/`; on `OSError` from mkdir OR either `open()` the launcher falls back to `subprocess.DEVNULL` with a single WARN log so MagicMirror failing-to-log never crashes the bot. FD-leak-safe: streams stored as `MagicMirrorProcess` attributes, closed in `stop()`'s `finally` block, partial-open failure closes the first stream before returning None. start.py integration (114 → 138 lines): new `spawn_magicmirror`/`stop_magicmirror` helpers mirror `spawn_syncall_daemon`/`stop_syncall_daemon` so test shapes stay identical; `main()` shutdown order is `stop_magicmirror(magicmirror_proc)` → `stop_syncall_daemon(syncall_proc)` → `dashboard_thread.join(5)`. `.env.example` gains `MAGICMIRROR_AUTOSTART_ENABLED=false` with a 4-line comment explicitly noting the flag is **orthogonal** to `MAGICMIRROR_ENABLED` and coordinates with sub-04's LaunchAgent choice (either in-process via this flag, OR a second plist with flag off). `workspace/MAGICMIRROR.md` gains a pure-suffix `## Auto-Launch on Mac` section (15 appended lines, byte-identical prefix through line 97 verified by `git diff f8aba52..HEAD`) covering the env-var table, shutdown order, log routing, the autostart-off manual `cd magicmirror && npm run server` path, and the flag-on-but-config-missing error. **Part A (audio device selection) intentionally NO-OP**: survey of `tts_engine.py`/`voice_delivery.py`/`voice_tools.py`/`voice_trigger_hook.py` confirmed Kokoro synthesizes WAV → PyAV converts to OGG/Opus → `SpeakTool` ships the OGG as a Telegram voice message — nothing on the host plays audio. Shipping `audio_config.py` would be dead code per anti-slop rule #1. `ADHD_AUDIO_OUTPUT_DEVICE` deferred to Task 19 (Whisper mic input) when it surfaces the consumer. 44 new tests across 3 files: `test_magicmirror_launcher_pure.py` (19 tests — argv shape, purity round-trip, no-shell-metachars grep, truthy-matrix including whitespace-strip and case-insensitive cells, flag-off returns None, config-missing raises with both substrings); `test_magicmirror_launcher_lifecycle.py` (18 tests — `_StubPopen` fixture, start/stop/kill via `HangingPopen.wait` raising `TimeoutExpired`, `test_stop_is_idempotent`, `test_start_raises_after_stop` matching "single-use", default logs dir + `ADHD_LOG_DIR` override + tilde-expansion via `monkeypatch.setenv("HOME", ...)`, `test_log_dir_unwritable_falls_back_to_devnull` with `caplog` WARN assertion and `Path.mkdir` monkeypatch, no-platform-branch grep, line-budget test); `test_start_py_magicmirror.py` (7 tests — flag-off/false return None, flag-on invokes Popen (conditionally `pytest.skip` when real `magicmirror/config/config.js` absent from checkout), RuntimeError propagates through `start.spawn_magicmirror`, `stop_magicmirror(None)` is no-op, `main()` wires stop-ordering verified by observed list `["magicmirror", "syncall"]`). Full suite: **1913 → 1956 passed, 14 → 15 skipped** (exactly +43 pass +1 skip = 44 new, zero regressions from sub-02). mypy --strict clean (`Success: no issues found in 1 source file`). Line-count budget: launcher 223, start.py 138, `.env.example` +4 lines, `MAGICMIRROR.md` 114 — all source ≤ 300; pure-test 103, start-py-test 172 ≤ 300; **lifecycle-test 304 lines exceeds both the description's ≤250 cap AND code-rules' ≤300 cap by 4 lines — flagged below**. Key design decisions: (1) `npm start` literal per AC #4 over Mac-correct `npm run server` — research section 2.4 flagged `magicmirror/package.json`'s `node --run start:wayland` won't run on macOS, but impl chose literal AC compliance and defers the real fix to sub-05's live smoke (either add `start:darwin` to package.json OR flip launcher to `["npm", "run", "server", "--prefix", ...]` — latter is the smaller diff); (2) case-insensitive flag check (`"True"` → True) deviates from AC #3 literal but matches family convention — documented tradeoff; (3) `MagicMirrorProcess` as plain class over frozen dataclass because the contract is stateful (_proc populates in start, _stopped flips in stop) — mirrors `PulseBundle`'s posture; (4) `launch_magicmirror` calls `start()` immediately (two-step construct/start semantics rejected) to match `spawn_syncall_daemon`'s "returns an already-running child" precedent; (5) one scoped `# type: ignore[assignment]` on `popen=subprocess.Popen` default for typeshed overload conflict, same pattern as `spawn_syncall_daemon`. Downstream contract: sub-04 (LaunchAgent) chooses wrap-start.py-with-flag-on OR wrap-start.py-with-flag-off-plus-separate-MagicMirror-plist — launcher provides the in-process option, doesn't commit; sub-05 live smoke triggers conditionally-skipped `test_spawn_magicmirror_flag_on_invokes_popen` and is the forcing function for the `npm start` vs `npm run server` resolution. Scope discipline: zero edits to `gateway_runner.py`, `hook_factory.py`, `magicmirror_setup.py`, or any engine/hook module — only `start.py`'s narrow integration touches pre-existing code. Pending LOW polish: `stop()` timeout could clamp negatives or raise explicit ValueError; `_close_streams()` could expand except clause to include ValueError for closed-file edge; `_StubPopen.instances` class-attribute-list could move to a shared conftest fixture; sub-05 must resolve the `npm start` Mac-runnability question and decide whether to invert AC #3 or amend the spec.
- Status: DONE
- Task: 18/sub-03
- Files: magicmirror_launcher.py, start.py, .env.example, workspace/MAGICMIRROR.md, tests/test_magicmirror_launcher_pure.py, tests/test_magicmirror_launcher_lifecycle.py, tests/test_start_py_magicmirror.py
- Depends on: [mac-install-script], [mac-data-paths-and-migration], [magicmirror-vendor-and-config], [custom-gateway-task16-smoke]
- Issues: (1) MEDIUM: `tests/test_magicmirror_launcher_lifecycle.py` = 304 lines, exceeds description's ≤250 cap by 54 lines AND code-rules' hard 300-line cap by 4 lines (AC #15 literal failure). Impl report's AC #15 row omitted test files from the enumeration. Fix: extract the `_StubPopen` class and `_make_config` helper to a shared `tests/conftest.py` (would shrink both `test_magicmirror_launcher_lifecycle.py` and `test_start_py_magicmirror.py` by ~40 lines each) OR consolidate the four tests covering "log stream kwargs end up on Popen" into one parametrized case. (2) MEDIUM: `magicmirror_launcher.py:40` `is_magicmirror_autostart_enabled` uses `.strip().lower() == "true"` so `"True"`/`"TRUE"`/`"tRuE"` all return True — deviates from AC #3's literal `"True" → False`. Documented in impl report decision #2 as family-consistency choice (matches `is_magicmirror_enabled`, `is_pulse_engine_enabled`, `is_syncall_enabled`, `is_gcal_enabled`). Two resolution paths: drop `.lower()` for one-character AC compliance (plus invert `test_autostart_enabled_case_insensitive`), or amend AC #3 wording. (3) LOW: `magicmirror_launcher.py:50` `build_magicmirror_command` emits `["npm", "start", ...]` but vendored `magicmirror/package.json` maps `npm start` → `node --run start:wayland` which won't run on macOS; documented-deferred to sub-05's live smoke per impl decision #1. Flag defaults to false so nothing breaks for opt-out users; `workspace/MAGICMIRROR.md` documents `cd magicmirror && npm run server` as the manual alternative. Sub-05 fix: either add `start:darwin` entry to `magicmirror/package.json` or flip `build_magicmirror_command` to `["npm", "run", "server", "--prefix", ...]` — latter is the smaller diff. (4) LOW: `magicmirror_launcher.py:129` `stop(timeout: float = 10.0)` accepts negative floats without guarding; `proc.wait(timeout=-1)` would raise `ValueError` downstream without a caller-friendly message. Fix: `if timeout < 0: raise ValueError(f"stop timeout must be non-negative, got {timeout}")` at method entry. Zero impact in practice — only internal callers pass 10.0. (5) LOW: `magicmirror_launcher.py:170` `_close_streams()` catches only `OSError` — `stream.close()` on an already-closed file object can raise `ValueError`. Expand to `except (OSError, ValueError)` for defensive symmetry with the rest of the module. (6) LOW: `tests/test_magicmirror_launcher_lifecycle.py:21` + `tests/test_start_py_magicmirror.py:15` declare `_StubPopen.instances: list = []` as a class attribute — autouse fixture resets it, but any future test using the class outside the fixture will leak state. Move to a conftest-yielded fixture. (7) INFO: `test_spawn_magicmirror_flag_on_invokes_popen` is conditionally `pytest.skip`ped when `magicmirror/config/config.js` absent (so it skips in CI without setup). Correct forcing function is sub-05's live-on-Mac smoke; verify the skip flips to pass after `setup_workspace.py MAGICMIRROR_ENABLED=true` runs on the Mac.
- Detail: _build/tasks/18-mac-deployment-port/sub-03/18-03x.md

### [mac-launchagent-service]
> Per-user `launchd` LaunchAgent stack turning `.venv/bin/python start.py` into a supervised auto-start service on macOS — one command installs, one uninstalls, one reports status; all idempotent. New `deploy/com.adhdassistant.bot.plist.template` (81 lines) is a static XML plist with five substitution placeholders (`${REPO_ROOT}` / `${HOME}` / `${ADHD_LOG_DIR}` / `${NANOBOT_TIMEZONE}` / `${EXTRA_ENV_ENTRIES}`) enumerated in the header comment; encodes the 10 required launchd keys — `Label=com.adhdassistant.bot`, two-element `ProgramArguments` (interpreter + entrypoint as absolute paths), `WorkingDirectory`, `EnvironmentVariables` dict seeded with `HOME`/`PATH=/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin`/`LANG=en_US.UTF-8`/`ADHD_LOG_DIR`/`NANOBOT_TIMEZONE` plus the `${EXTRA_ENV_ENTRIES}` injection slot, `StandardOutPath`/`StandardErrorPath` under `${ADHD_LOG_DIR}`, `RunAtLoad=true`, `KeepAlive={SuccessfulExit: false}` (dict form so clean `launchctl unload` does NOT retrigger), `ThrottleInterval=30` (crash-loop guard), `ProcessType=Interactive`. `scripts/install_launchagent.sh` (148 lines, `set -euo pipefail` + ERR-trap with LINENO) resolves `REPO_ROOT` via `cd "$(dirname "$0")/.." && pwd`, reads `ADHD_LOG_DIR` + `NANOBOT_TIMEZONE` from `.env` through a pipefail-safe `grep | tail | cut || true` helper, tilde-expands with bash-3.2-safe `${value/#\~/$HOME}` (launchd does NOT expand `~` in plist paths — sub-02's flagged footgun), `mkdir -p` on both the log dir and `~/Library/LaunchAgents/`, builds `EXTRA_ENV_ENTRIES` from a 13-key whitelist (`PULSE_ENGINE_ENABLED`, `DREAM_STATE_ENABLED`, `DREAM_STATE_CRON`, `VOICE_AUTO_ENABLED`, `VOICE_DISCO_ENABLED`, `GOOGLE_CALENDAR_ENABLED`, `MAGICMIRROR_ENABLED`, `MAGICMIRROR_AUTOSTART_ENABLED`, `TASKWARRIOR_ENABLED`, `SYNCALL_ENABLED`, `ADHD_DATA_DIR`, `DASHBOARD_DATA_DIR`, `TASKWARRIOR_DATA_DIR`) — each value tilde-expanded and XML-escaped (`&`/`<`/`>`), emitted as `<key>…</key><string>…</string>` into a temp file cleaned by EXIT-trap; renders via `sed -e "s|\${…}|…|g"` with `|` delimiter and `/\${EXTRA_ENV_ENTRIES}/r FILE` + `/…/d` for multi-line insertion; `launchctl unload … 2>/dev/null || true` (idempotent) then `launchctl load -w`; verifies with `launchctl list | grep com.adhdassistant.bot`; prints a banner naming plist path, log paths, status command, and uninstall command. `scripts/uninstall_launchagent.sh` (23 lines) unloads tolerantly and `rm -f`s the plist. `scripts/launchagent_status.sh` (45 lines) reads `ADHD_LOG_DIR` with same `.env` fallback, greps `launchctl list`, tails 20 lines of both log files, exits 0 when PID is numeric OR `LastExitStatus == 0`. **Anti-leak boundary**: no secrets in the plist — `OPENROUTER_API_KEY` / `TELEGRAM_BOT_TOKEN` / `TELEGRAM_USER_ID` grep-asserted absent from the install script (plist lives under world-readable `~/Library/LaunchAgents/`); tokens stay in `~/.nanobot/config.json` which nanobot reads itself. **No MagicMirror plist shipped** — description §5's conditional resolved against sub-03's in-process launcher posture; `MAGICMIRROR_AUTOSTART_ENABLED` and `MAGICMIRROR_ENABLED` are on the whitelist so the in-process path receives them. **Re-run contract**: `.env` edits require re-running `install_launchagent.sh` — documented in LAUNCHAGENT.md Troubleshooting. `workspace/LAUNCHAGENT.md` (165 lines) ships via `setup_workspace.TEMPLATE_FILES` (single-line insert between `MAC_PATHS.md` and `states.yaml`) with 7 sections (What is a LaunchAgent / Prerequisites / Install / Uninstall / Check status / Reading logs / Troubleshooting / Rollback) covering the three most-likely failure modes plus quick and last-resort (Mac → Windows rsync) rollback paths. 62 new tests across 3 files: `test_launchagent_plist_template.py` (214 lines, 25 tests — template exists + ≤120-line cap + header enumerates every placeholder, `ET.fromstring` parses both with and without extra entries, `plistlib.loads` succeeds, all 10 required keys present, `Label == "com.adhdassistant.bot"`, `ProgramArguments` is 2-element `[…/.venv/bin/python, …/start.py]` absolute, `WorkingDirectory` absolute, `EnvironmentVariables` has core keys + `/opt/homebrew/bin` in PATH + injected extras surface, log paths under dummy log dir, `RunAtLoad is True`, `KeepAlive` is dict with `SuccessfulExit is False`, `ThrottleInterval` is `int >= 30` excluding `bool`, `ProcessType == "Interactive"`, this file never shells out to launchctl); `test_install_launchagent_script.py` (244 lines, 32 tests — all three scripts exist + line caps (200/80/80) + `bash -n` parse (`pytest.skip` when bash absent), shebang `#!/usr/bin/env bash`, `set -euo pipefail` within first 10 lines, zero `sudo` across all three, install: unload-before-load + `2>/dev/null` + `|| true` + `-w` + `launchctl list … com\.adhdassistant\.bot` regex + `mkdir -p` + sed + `#\~/$HOME` tilde idiom + `.env` reference + `${REPO_ROOT}/logs` fallback + banner names `uninstall_launchagent.sh` + `bot.err.log` + target plist path + ERR trap, uninstall: unload + `|| true` + `rm -f` + mentions `adhd-assistant`, status: `launchctl list … com.adhdassistant.bot` + tails both log files, 13-key whitelist pinned + 3 secret names absent, this file never shells out to launchctl); `test_launchagent_md_deploy.py` (33 lines, 5 tests — manifest contains + listed exactly once + source exists + `copy_workspace_files(tmp)` produces it + byte-identical `read_text` round-trip). Full suite: **1956 → 2018 passed, 15 skipped** (exactly +62, zero regressions from sub-03 baseline). `mypy --strict` clean on all three new test files. Line-count budget: plist 81/120, install 148/200, uninstall 23/80, status 45/60, tests 214/244/33 — all within caps. Scope discipline: `git show 858582e --stat` confirms `custom_gateway.py` / `gateway_runner.py` / `start.py` / `hook_factory.py` / every engine/hook module untouched; `setup_workspace.py` diff is a 2-line reorder + one-entry insert into `TEMPLATE_FILES`. **Key design decisions**: (1) bake a 13-key `.env` whitelist into the plist `EnvironmentVariables` at install time (research approach 1) — rejected approaches were wrapper shell script (would violate AC #5's 2-element `ProgramArguments` shape), silent-default (silent feature-flag regression under launchd), and patching `start.py` (forbidden by AC #15); (2) `sed` over `envsubst` (not guaranteed on stock macOS) or bash-only param expansion (mixes styles with multi-line entry insertion) or Python helper (wrong layer — runs before venv Python guaranteed); (3) `launchctl load`/`unload` pair over modern `bootstrap`/`bootout` — literal AC #6 match, old pair still supported on macOS 14+; (4) `KeepAlive` dict with `SuccessfulExit: false` so `launchctl unload` exits cleanly without retrigger; (5) `ThrottleInterval=30` over shorter intervals — protects against tight import-error crash loops. Downstream contract for sub-05: runs `bash scripts/install_launchagent.sh` live on Mac, asserts `launchctl list` shows `com.adhdassistant.bot` with numeric PID within N seconds + logs populate under `$ADHD_LOG_DIR`; then runs `bash scripts/uninstall_launchagent.sh`, asserts label gone + plist file gone; `bash scripts/launchagent_status.sh` exits 0 while loaded and non-zero after uninstall. Sub-05 also owns the deferred `.expanduser()` patch to `custom_gateway.resolve_data_dir` (flagged by sub-02) so literal `~/Library/...` values in `.env` don't create a `~` directory under the repo.
- Status: DONE
- Task: 18/sub-04
- Files: deploy/com.adhdassistant.bot.plist.template, scripts/install_launchagent.sh, scripts/uninstall_launchagent.sh, scripts/launchagent_status.sh, workspace/LAUNCHAGENT.md, setup_workspace.py, tests/test_launchagent_plist_template.py, tests/test_install_launchagent_script.py, tests/test_launchagent_md_deploy.py
- Depends on: [mac-install-script], [mac-data-paths-and-migration], [magicmirror-autostart-launcher], [nanobot-workspace-setup]
- Issues: (1) LOW: `scripts/install_launchagent.sh:112-117` sed uses `|` as delimiter; if any of `REPO_ROOT` / `HOME` / `ADHD_LOG_DIR` / `NANOBOT_TIMEZONE` contained `|` the render would fail. Realistic macOS paths and IANA timezones never contain `|`; `ADHD_LOG_DIR` is user-controlled through `.env` but still extremely unlikely. Fix: escape `|` in each value before sed, or switch to Python rendering. (2) LOW: `scripts/install_launchagent.sh:112-115` applies `xml_escape` only to values written into `EXTRA_ENV_ENTRIES` (lines 84–90), not to the four single-line placeholders written into `<string>…</string>` blocks. A hostile `.env` `ADHD_LOG_DIR` containing `<`/`>`/`&` would produce malformed XML and `launchctl load` would fail with a parse error rather than a readable message. Fix: `ADHD_LOG_DIR="$(xml_escape "${ADHD_LOG_DIR}")"` after the tilde expansion (and symmetrically for `NANOBOT_TIMEZONE`). (3) LOW: `scripts/launchagent_status.sh:40-44` exit-code heuristic returns 0 when `last_exit == "0"` even if the service has crashed cleanly and is mid-`ThrottleInterval` — matches description step 4 literal ("exits 0 if loaded + last exit was 0 or still running") so not a spec violation, but a future reader may want the tighter condition "pid numeric AND last exit 0". Informational only. (4) LOW: `scripts/install_launchagent.sh:93` the cleanup `trap 'rm -f "${EXTRA_ENTRIES_FILE}"' EXIT` silently overrides any future EXIT trap a maintainer might add; preserves the line-9 ERR trap but no comment explains the precedence. One-line comment would help.

### [mac-smoke-test-and-closure]
> Task 18 closure subtask — Mac-only smoke test pair + user-facing runbook + closure of the sub-02 deferred `.expanduser()` footgun. New `tests/test_mac_smoke.py` (60 lines, baseline + shutdown) and `tests/test_mac_smoke_phase2.py` (176 lines, seven Phase 2 flag cells + LaunchAgent rollback) both module-skip on `sys.platform != "darwin"` — the one description-sanctioned `sys.platform` branch in the repo (AC #1 + #12). Shared `tests/_mac_smoke_harness.py` (151 lines) exports `run_until_markers(markers, env)` — spawns `.venv/bin/python start.py` under repo cwd with fake creds merged into env, tails merged stdout+stderr through `subprocess.Popen.stdout.readline` with a 60-second watchdog, matches marker substrings into a per-marker `markers_seen` dict, then sends SIGINT and `communicate(timeout=15)`s; if shutdown exceeds 15 s raises `AssertionError` naming the deadline (not `TimeoutExpired`) per AC #4. `BASELINE_MARKERS` is a 7-tuple pinning the 2026-04-16 log shape (gateway start, "Custom gateway ready: 6 hooks", dashboard bound on 0.0.0.0:8085, four tool-registration log lines summing to 14 tools). `FAKE_CREDENTIALS` ships fake `OPENROUTER_API_KEY` / `TELEGRAM_BOT_TOKEN` / `TELEGRAM_USER_ID` so `validate_env_vars` passes but Telegram `getMe` fails hermetically. `TestBaselineSmoke` class: three tests cover markers-appear, no-traceback, and graceful-shutdown-within-deadline (AC #2 + #4). `TestPhase2FlagCells` class: one cell per Phase 2 adapter (MagicMirror / MagicMirror-Autostart / Calendar / Taskwarrior / Pulse / Dream / syncall) — each toggles one flag, `pytest.skip`s when its prerequisite is missing (e.g. MagicMirror `config/config.js` absent, Taskwarrior `task` binary not on PATH per `shutil.which`). `TestLaunchAgentRollback` class: `test_install_then_uninstall_leaves_no_residue` runs `bash scripts/install_launchagent.sh` via subprocess, asserts plist exists + `launchctl list` contains `com.adhdassistant.bot`, then always-runs uninstall in `finally`, asserts plist gone + label gone (AC #13); refuses to run if `~/Library/LaunchAgents/com.adhdassistant.bot.plist` already exists (clobber guard for a user with a real install). **Deferred sub-02 `.expanduser()` patch lands here**: `custom_gateway.py:72,80` (`resolve_data_dir` / `resolve_states_path`) + `dashboard_api.py:61-62` (`DASHBOARD_DATA_DIR` / `DASHBOARD_STATIC_DIR`) now call `.expanduser()` on env-var values — two-line change each, no-op on non-tilde strings so the existing Windows tests that set `ADHD_DATA_DIR=<tmp_path>/data` continue to pass. Four TDD tests (`test_custom_gateway.py::TestResolveDataDir::test_env_override_expands_tilde`, `TestResolveStatesPath::test_env_override_expands_tilde`, `test_dashboard_api.py::TestLoadConfigFromEnv::test_data_dir_expands_tilde`, `test_static_dir_expands_tilde`) assert `str(result).startswith(str(Path.home()))` cross-platform (works on Windows because `Path.home()` returns `C:\Users\<user>` and `expanduser` handles it). `workspace/MAC_DEPLOYMENT.md` (150 lines) user-facing runbook ships via `setup_workspace.TEMPLATE_FILES` insert (line 79, between `LAUNCHAGENT.md` and `states.yaml`): TL;DR is five copy-paste commands (Homebrew install → `bash install_mac.sh` → `.env` edit → `migrate_windows_to_mac.py --apply` → `install_launchagent.sh`), plus sections on Verification (what to grep in `bot.out.log` + LAN dashboard URL + `launchctl list`), Start/Stop/Restart (`launchctl kickstart -k` + `unload`/`load`), Where-things-live table (8 Mac-conventional paths including the hardcoded `~/.gcal_credentials.pickle` syncall pickle), Troubleshooting (5 failure modes: `.env` missing, OAuth expired, Taskwarrior not on LaunchAgent PATH, MagicMirror port clash, dashboard port clash), Rollback (`uninstall_launchagent.sh` → `migrate_windows_to_mac.py --reverse`), and Cost posture table restating the $7/month ceiling by feature flag (AC #14). `README.md` surgical 8-line diff: top banner "Deployment target: macOS Air M2" blockquote at line 3, Mac callout after Prerequisites at line 144, Platform Support table updated — macOS row → "Deployment target — runs bot + dashboard 24/7 via LaunchAgent", Windows row → "Development only — test suite passes, not intended for production use". All other sections byte-identical (AC #8). `tests/test_mac_deployment_md_deploy.py` (33 lines, 5 cases: manifest-contains + listed-exactly-once + source-exists + `copy_workspace_files(tmp)` produces it + byte-identical `read_text` round-trip — mirrors the four sibling deploy tests). Full Windows suite: **2018 → 2027 passed, 15 → 17 skipped** (+9 pass = 2 tilde/custom_gateway + 2 tilde/dashboard_api + 5 deploy tests; +2 skip = both Mac smoke modules); matches AC #9 delta exactly. Line-count budget: 60 / 176 / 151 / 33 / 150 — all under 300 (AC #15). Impl acknowledges live-Mac smoke is DEFERRED (this pipeline ran on Windows per description dependency 5 — the Mac first run is the forcing function for MagicMirror `npm start` vs `npm run server` resolution carried from sub-03 and for the two smoke-test marker issues flagged below). Scope discipline: zero edits to `gateway_runner.py` / `hook_factory.py` / `start.py` / any engine/hook module; `custom_gateway.py` / `dashboard_api.py` / `setup_workspace.py` / `README.md` each receive surgical diffs (≤5 lines of code each). Two commits: `3bbe074` (fix: expand tilde in ADHD_DATA_DIR/ADHD_STATES_PATH/DASHBOARD_*_DIR) + `7fab37b` (task 18-05: add Mac smoke tests + MAC_DEPLOYMENT.md runbook). **Task 18 closure posture**: this subtask satisfies the Task-level promise ("Mac is the canonical deployment host from here forward") by composing sub-01..04 into a proof-of-deployment test harness; the Task-level `task-verify.md` (description Part C.1) remains outstanding as a separate verify-agent pass.
- Status: DONE-WITH-ISSUES
- Task: 18/sub-05
- Files: tests/test_mac_smoke.py, tests/test_mac_smoke_phase2.py, tests/_mac_smoke_harness.py, tests/test_mac_deployment_md_deploy.py, workspace/MAC_DEPLOYMENT.md, custom_gateway.py, dashboard_api.py, setup_workspace.py, README.md, tests/test_custom_gateway.py, tests/test_dashboard_api.py
- Depends on: [mac-install-script], [mac-data-paths-and-migration], [magicmirror-autostart-launcher], [mac-launchagent-service], [nanobot-workspace-setup], [dashboard-api-wiring], [pulse-dream-closure], [taskwarrior-store-backend], [gcal-mcp-server-wiring]
- Issues: (1) MEDIUM: `tests/test_mac_smoke_phase2.py:103` passes `"Pulse engine started"` as a marker, but that string does not exist in the codebase — the actual pulse-start log (`pulse_gateway_setup.py:184`) is `"pulse.start concern_count=%d tz=%s flag=True dream=%s"`. On Mac the marker loop will burn the full 60-second watchdog, then `_assert_no_traceback` is the only assertion — so the test "passes" without proving the pulse engine started. Fix: replace with `"pulse.start"` substring and add an explicit `assert "pulse.start" in result.stdout` after the no-traceback check. (2) MEDIUM: `tests/_mac_smoke_harness.py:38` bakes `"Custom gateway ready: 6 hooks"` into `BASELINE_MARKERS`, but three Phase 2 cells enable flags that change the hook count (calendar → 7, MagicMirror + MagicMirror-autostart likely → 7 each). `run_until_markers` does not fail on unseen markers, so those tests also burn the full 60-second boot window and rely on post-loop assertions to pass — a regression in a non-hook baseline marker (e.g. dashboard bind) would be masked by the same wait. Fix: split into `BASELINE_BOOT_MARKERS` (flag-invariant) and a separate `BASELINE_HOOK_COUNT` used only by `TestBaselineSmoke`; each Phase 2 cell uses boot markers + its own per-flag expectation. (3) LOW: `test_magicmirror_flag_on_logs_ready` / `test_taskwarrior_flag_on_selects_backend` / `test_pulse_flag_on_starts_engine` / `test_dream_flag_on_instantiates_engine` only assert `_assert_no_traceback` — weaker than description AC #3's per-flag observables. Add one positive assertion each (e.g. Taskwarrior → grep for `TASKWARRIOR_ENABLED=true` or `TaskwarriorStore`, Dream → grep for `dream.*enabled`). (4) LOW: `tests/_mac_smoke_harness.py:104-116` `run_until_markers` swallows missing-marker watchdog expiry silently; only `test_baseline_markers_appear` inspects `result.markers_seen`. Root cause of MEDIUM-1 / MEDIUM-2 passing-by-accident. Fix: add optional `required=True` parameter defaulting to strict, or assert per-caller. (5) LOW: `tests/_mac_smoke_harness.py:107` blocking `stdout.readline()` — a hang inside Python's logging-flush path between marker emissions would overrun the 60-second watchdog; unlikely in practice. (6) INFO: The Task-level `_build/tasks/18-mac-deployment-port/task-verify.md` (description Part C.1) is NOT written here — it is a Task-level closure artefact reserved for a separate verify-agent pass. (7) INFO: AC #16 mypy --strict claim not re-verified on the verify host (mypy install state unknown); trusting the impl report for this pass.
- Detail: _build/tasks/18-mac-deployment-port/sub-05/18-05x.md

### [whisper-transcription-backends]
> Single Python module exposing three Whisper backends (local faster-whisper, OpenAI hosted, Groq hosted) behind one async `TranscriptionBackend` Protocol; selected at runtime from `TRANSCRIPTION_BACKEND` env var; structured `TranscriptionResult` carries an optional 0–1 `confidence` float when the backend can produce one. Local backend lazily constructs `faster_whisper.WhisperModel` on first call (constructor accepts `model_factory` for DI in tests), wraps the sync `transcribe` in `asyncio.to_thread`, and materialises the segment generator inside the worker so iteration never re-enters the event loop. Hosted backends (OpenAI `whisper-1` + Groq `whisper-large-v3`) share `_HostedWhisperBackend` which POSTs multipart form via `httpx.AsyncClient` with optional `transport: AsyncBaseTransport | None` test seam; raises `RuntimeError` with provider+status on 4xx/5xx and on `httpx.RequestError`, raises `RuntimeError` on missing API key naming the env var, raises `FileNotFoundError` on missing audio path. Pure helpers: `compute_confidence(segments) -> float` = `mean(clamp(exp(avg_logprob), 0, 1) * (1 - no_speech_prob))` clamped to `[0, 1]` (empty → 0.0); `resolve_low_confidence_threshold(env)` reads `WHISPER_LOW_CONFIDENCE_THRESHOLD`, falls back to `LOW_CONFIDENCE_THRESHOLD = 0.4`, clamps to `[0, 1]`; `is_low_confidence(result, threshold)` returns `False` when `result.confidence is None` (passthrough — never silently reject low-confidence transcriptions per task-spec ADHD constraint), else `result.confidence < threshold` (strict less-than, equality is not low). Factory `build_transcription_backend(env)` does `if/elif/else` dispatch on lowercased+stripped `TRANSCRIPTION_BACKEND` (default `"local"`); unknown values raise `ValueError` naming the offending value and listing all three valid options. `transcription_backend.py` is 299 lines (under the 300 cap), `mypy --strict` clean, zero `Any`, zero `sys.platform`/`os.name` branches, zero `except Exception` (only catches `httpx.HTTPStatusError`, `httpx.RequestError`, `FileNotFoundError`). Module-level imports are stdlib + `httpx` only — `faster_whisper` is imported lazily inside `_load_whisper_model_class()` via `importlib.import_module` + `cast(_WhisperModelFactory, module.WhisperModel)` so the module loads cleanly on hosts without faster-whisper. Internal `_SegmentLike` / `_TranscriptionInfoLike` / `_WhisperModelLike` Protocols duck-type faster-whisper's NamedTuples + `WhisperModel` so the module type-checks without importing the package and without `Any`. Form-multipart sent as `files=` + separate `data={"model": …}` (mypy strict caught the heterogeneous-files-dict issue; httpx merges both into one body, identical wire payload). `requirements.txt` adds `# Voice input (Task 19)` comment + `faster-whisper>=1.0.0`. 52 new tests across three files (29 pure / 14 hosted / 9 local), all model-mocked (no real model download, no real network — hosted uses `httpx.MockTransport`); test helper `_run(coro)` wraps `asyncio.run` per repo convention. Lazy-load failure path covered by monkeypatching `transcription_backend.importlib.import_module`. Full suite: **2027 → 2079 passed, 17 skipped (unchanged)** in 22.5 s — exactly +52 / 0 regressions / 0 new skips. Smoke verified: `python -c "from transcription_backend import build_transcription_backend; b = build_transcription_backend({'TRANSCRIPTION_BACKEND': 'groq', 'GROQ_API_KEY': 'test'}); print(type(b).__name__)"` → `GroqWhisperBackend`. **Out of scope** (sub-02's job): no edits to `gateway_runner.py` / `custom_gateway.py` / `hook_factory.py` / `start.py` / any `*_hook.py` / any engine module / `workspace/SOUL.md` / `workspace/HEARTBEAT.md` / `workspace/config.json.template` / `.env.example` / `setup_workspace.py`. **Exports for sub-02**: `TranscriptionBackend` (Protocol), `TranscriptionResult` (frozen dataclass), `build_transcription_backend(env)`, `is_low_confidence(result, threshold)`, `resolve_low_confidence_threshold(env)`, `LOW_CONFIDENCE_THRESHOLD` constant, plus the URL/model constants `OPENAI_TRANSCRIPTIONS_URL` / `OPENAI_MODEL` / `GROQ_TRANSCRIPTIONS_URL` / `GROQ_MODEL`. **Sub-03 contract**: SOUL.md guidance will reference `LOW_CONFIDENCE_THRESHOLD` + the passthrough rule pinned in `is_low_confidence`. Three commits: `c5e326b` (module + dep), `0d14bdc` (test suites), `3f5cb17` (impl report).
- Status: DONE
- Task: 19/sub-01
- Files: transcription_backend.py, requirements.txt, tests/test_transcription_backend_pure.py, tests/test_transcription_backend_hosted.py, tests/test_transcription_backend_local.py
- Depends on: [nanobot-workspace-setup]
- Issues: (1) LOW: AC8 wording inconsistency — AC text says "exactly one new line: faster-whisper>=1.0.0" but description body §5 says "single line, under # Voice input (Task 19) comment", which natural-reads as comment + dep. Implementation added 2 lines (comment + dep). No fix needed; both AC sentences are internally inconsistent and the implementation honours the body text. (2) LOW: hosted backend checks `api_key` before `audio_path.exists()` (`transcription_backend.py:194-203`); if both fail the caller sees `RuntimeError` instead of `FileNotFoundError`. Description does not pin order; reasonable defence-in-depth. No fix needed. (3) LOW: `compute_confidence` WHY documentation is a 4-line docstring (`transcription_backend.py:79-85`), not a literal "one-line WHY comment" as AC4 says — more useful than a one-line comment, conforms to code-rules.md spirit. No fix needed. (4) LOW (acknowledged): local-backend tests do NOT use `pytest.importorskip("faster_whisper")` per description §4 — every test injects a `model_factory`, the lazy-load failure path is covered via `monkeypatch.setattr(transcription_backend.importlib, "import_module", fake_import)`. Deviation gains 8 test cases and the suite still runs offline; documented in impl report decision #10. No fix needed. (5) LOW: tests reach into private state `backend._model_size` / `_compute_type` / `_device` (`tests/test_transcription_backend_local.py:87-89`) — acceptable internal-state introspection in unit tests. (6) INFO: `if self._transport is not None` branch in `_post_transcription` (`transcription_backend.py:215-220`) is the only minor structural duplication — could collapse to a single `httpx.AsyncClient(timeout=..., transport=self._transport)` call since httpx accepts `transport=None`. Cosmetic; current form is explicit and tested.
- Detail: _build/tasks/19-whisper-voice-input/sub-01/19-01x.md

### [voice-input-channel-integration]
> Monkey-patches each nanobot channel's `transcribe_audio` method at gateway startup so incoming voice messages route through the sub-01 `[whisper-transcription-backends]` factory. `voice_input_integration.py` (262 lines, mypy --strict clean, zero `Any`, zero `sys.platform`/`os.name`, zero `except Exception`) exports a single entry point `setup_voice_input(env, channels, *, backend_builder=None) -> int` — flag-gated on `VOICE_INPUT_ENABLED=true` (strict `== "true"` via `is_voice_input_enabled`, mirroring `pulse_checkin_store.is_pulse_engine_enabled`), returns count of patched channels (0 when OFF), `backend_builder` is a test seam (production uses `build_transcription_backend` lazily so flag-OFF boot never drags `faster_whisper` into `sys.modules` — AC #2 verified by subprocess probe). The patched closure `_build_patched_transcribe` (a) estimates duration via `estimate_audio_duration_seconds(path) = st_size / VOICE_BYTES_PER_SECOND` where `VOICE_BYTES_PER_SECOND = 2048` (≈16 kbps OGG Opus ceiling — biases toward over-estimating so borderline files reject rather than admit past the cap, avoiding a mutagen/ffmpeg-probe dependency), returning `None` for missing/zero-byte files (which MUST NOT reject), (b) short-circuits to `render_duration_rejection(max_seconds) = "voice too long — limit {seconds}s"` when duration is known AND exceeds `MAX_VOICE_DURATION_SECONDS` (default 180, env-overridable via `resolve_max_voice_duration(env)` which clamps `< 1` to 1 and raises `ValueError` on non-integer), (c) `await backend.transcribe(path)` via the sub-01 Protocol, (d) renders via `render_transcription_text(result, threshold)` which does `" ".join(result.text.split())` whitespace-collapse + prefixes `LOW_CONFIDENCE_PREFIX = "(low confidence) "` when `is_low_confidence(result, threshold)` is True (strict `<`, `None` confidence passes through unprefixed). Catches EXACTLY four exceptions — `FileNotFoundError`, `RuntimeError`, `httpx.RequestError`, `httpx.HTTPStatusError` (note: the last two are siblings in httpx, NOT parent/child, so both must be listed) — converting to `FAILED_MARKER = "failed to process voice message"` (non-empty so upstream `_download_message_media` wraps as `[transcription: failed to process voice message]` rather than dropping to `[voice: /path]`); `asyncio.CancelledError` propagates (test `test_cancelled_error_propagates` asserts). Backend-error WARNs are rate-limited 1/hr per channel via module-level `_last_error_log_at: dict[str, datetime]` + injectable `clock: Callable[[], datetime]` test seam + `_reset_rate_limit_state()` autouse fixture, mirroring `magicmirror_hook` pattern. `install_voice_transcription(channels, backend, max_duration, threshold, clock=None)` per-instance `setattr(channel, "transcribe_audio", closure)` monkey-patch (lands on instance `__dict__`, shadows the class method — attribute lookup resolves instance first, no descriptor protocol invocation so closure receives `(file_path)` cleanly without spurious `self`); skips channels lacking the attribute; returns patch count. Returned text is RAW — never wrapped in `[transcription: ...]` by us — so nanobot's own `_download_message_media` wrapper produces `[transcription: (low confidence) ...]` when prefix fires (no double-wrap; sub-03's SOUL.md guidance matches exactly this bracketed form). Gateway wiring is surgical: one import line + one call `setup_voice_input(env_map, channels.channels)` after `ChannelManager(config, bus)` and before `_setup_heartbeat`; `gateway_runner.py` stays at 299 lines (was 300 cap, impl compressed `_pick_heartbeat_target` signature to reclaim one line). `.env.example` appended 7 entries under banner `# === Voice Input (Task 19) ===`: `VOICE_INPUT_ENABLED=false`, `TRANSCRIPTION_BACKEND=local`, `WHISPER_MODEL_SIZE=base`, `WHISPER_COMPUTE_TYPE=int8`, `WHISPER_DEVICE=` (empty=auto), `WHISPER_LOW_CONFIDENCE_THRESHOLD=0.4`, `MAX_VOICE_DURATION_SECONDS=180`. 54 new tests across two files (41 pure `test_voice_input_integration_pure.py` + 13 gateway `test_gateway_runner_voice_input.py`), all passing; full suite 2079 → 2133 passed, 17 skipped unchanged — exactly +54 / 0 regressions. AC #2 anchor: `TestNoFasterWhisperImportWhenFlagOff` boots a fresh Python subprocess, imports `voice_input_integration`, calls `setup_voice_input({'VOICE_INPUT_ENABLED': 'false'}, {})`, asserts `'faster_whisper' in sys.modules is False`. **Exports consumed by sub-03 + sub-04**: `setup_voice_input` (gateway entry), `install_voice_transcription`, `is_voice_input_enabled`, `LOW_CONFIDENCE_PREFIX`, `FAILED_MARKER`, `OVER_DURATION_MARKER_TEMPLATE`, `render_transcription_text`, `resolve_max_voice_duration`. **Sub-03 contract**: SOUL.md guidance will reference the literal string `[transcription: (low confidence) ` as the marker to detect — the prefix lives INSIDE the text, upstream wraps it, so the bracketed concatenation is what the LLM sees. **Out of scope and untouched**: `custom_gateway.py`, `hook_factory.py`, `start.py`, any `*_hook.py`, any engine/store module, `workspace/SOUL.md`, `workspace/HEARTBEAT.md`, `workspace/config.json.template`, `requirements.txt`, `setup_workspace.py`, anything under `references/`. Two commits: `a5f3b9f` (module + gateway wire + tests + .env.example) and `ba882a4` (impl report).
- Status: DONE
- Task: 19/sub-02
- Files: voice_input_integration.py, gateway_runner.py, .env.example, tests/test_voice_input_integration_pure.py, tests/test_gateway_runner_voice_input.py
- Depends on: [whisper-transcription-backends]
- Issues: (1) LOW: `voice_input_integration.py:45` declares `MAX_VOICE_DURATION_SECONDS: int = DEFAULT_MAX_VOICE_DURATION_SECONDS` as a public constant but the module itself never reads it (all callers go through `resolve_max_voice_duration`). Listed as a required export in the description, so the impl honours the spec — but it reads as redundant "just in case" surface. No fix. (2) INFO: gateway-side flag gate lives INSIDE `setup_voice_input` (so `gateway_runner.py:139` is a single unconditional call) rather than as an `if is_voice_input_enabled(env_map):` block at the call site as the description literally phrases it. AC #7 explicitly allows the "one helper call" form ("single `if` block plus (if needed) one helper call — not a sprawl") and observable behaviour is identical. No fix. (3) INFO: `VOICE_BYTES_PER_SECOND = 2048` heuristic calibrates for OGG Opus ~16 kbps — denser formats (MP3, WAV) register over-duration sooner than wall-clock; deliberate trade-off documented in module docstring to avoid a mutagen/ffmpeg-probe dependency for a 2-line helper. No fix. (4) INFO: `render_transcription_text` with empty `result.text` returns `""`; upstream then drops to `[voice: /path]`. Sensible — empty Whisper output + zero-length low-confidence marker would be absurd. No fix. (5) INFO: `is_voice_input_enabled` requires the strict string `"true"` (`"1"`/`"yes"`/`"on"` resolve False) matching the project's canonical `is_<feature>_enabled` idiom. Deliberate.
- Detail: _build/tasks/19-whisper-voice-input/sub-02/19-02x.md

### [voice-input-soul-guidance]
> Personality-layer guidance teaching the LLM how to read sub-02's voice markers. `workspace/SOUL.md` gains a single new `## Voice Input` H2 section (50 body lines, under the 65-line cap) inserted between `## Pulse + Dream` and `## Calendar` — chosen as the closest neighbour of the original description anchor (`## Fire Tablet Display` → `## Calendar`) once Task 16 and Task 17 had intervened; description's guardrail against re-ordering was honoured. The section contains a TL;DR paragraph plus six required `### ` subheadings in declared order: `### How Voice Input Arrives`, `### When Transcription Is Low Confidence`, `### ADHD Speech Is Valid`, `### Voice + Tasks / Memory / Buffers`, `### When Transcription Fails`, `### When Voice Is Too Long`. All four sub-02 marker substrings appear inside the section body: `[transcription:`, `(low confidence) ` (trailing space preserved), `failed to process voice message`, and the em-dash-sensitive `voice too long \u2014 limit` (U+2014 matches `OVER_DURATION_MARKER_TEMPLATE` byte-for-byte). Low-confidence tone mirrors the existing `### When Time Parsing Fails` pattern: do NOT silently guess, do NOT reject, do NOT force retyping — read the transcript back and confirm before acting. ADHD-speech bullets normalise mumbles, trail-offs, mid-sentence self-correction (LATEST version is the user's intent), topic switches, long pauses; clarifications go through ICNU framing (Task 02 motivation channels), not directives. `workspace/HEARTBEAT.md` gains a trailing `## Voice-Originated Inputs` H2 (8 body lines, under the 18-line cap) mentioning both `[transcription:` wrapping happens before hooks run (state detection, memory injection, scheduling see plain text) and `VOICE_AUTO_ENABLED` (Task 07) is **orthogonal** to voice INPUT — auto-voice OFF + voice-input ON is a valid combination. Tests: `tests/test_soul_voice_input_section.py` (215 lines, 40 tests) covering heading presence + position, subheading order, marker substrings including explicit em-dash absence guards (ASCII `-` and U+2013 en-dash rejected in the over-duration phrase), section line cap, TL;DR-starts-with-`[transcription:`, 11 banned-phrase roots from `tests/test_personality.py::BANNED_ROOTS`, and a parametrized "first three non-blank body lines" snapshot over all 17 pre-existing `## ` sections — the fuzzy byte-identity guard from description AC #6. `tests/test_heartbeat_voice_input_note.py` (152 lines, 13 tests) covers heading presence + is-last-section, `[transcription:` + `VOICE_AUTO_ENABLED` + literal `orthogonal` substring, line cap, non-empty body, and 7 pre-existing HEARTBEAT sections' first-three-lines snapshot. Both new test files `mypy --strict` clean. Scope deviation documented: `tests/test_soul_dream_section.py` bumped two constants (`PRE_EDIT_BYTES: 20000 → 24885` and `EXPECTED_HEADING_ORDER` gains `"## Voice Input"` between Pulse+Dream and Calendar) — mechanical updates required for the suite to stay green; authorised by that test's own docstring pattern "bump the baseline with intent". Zero runtime/production `.py`/`.json`/`.env.example`/`requirements.txt`/`setup_workspace.py` edits; `setup_workspace.py::TEMPLATE_FILES` already includes both workspace files so the new guidance ships without manifest changes (AC #11). Full suite: 2133 → **2191 passed, 17 skipped** (+58 / 0 regressions) — 40 from SOUL test file + 13 from HEARTBEAT test file + 5 previously-deselected fixtures now collected. Three commits: `3e62489` (workspace edits + tests), `4d1ea84` (impl report), plus the constants bump in `3e62489`. Sub-04 end-to-end tests will assert the new SOUL.md `## Voice Input` heading is present and well-formed; this entry satisfies that contract.
- Status: DONE
- Task: 19/sub-03
- Files: workspace/SOUL.md, workspace/HEARTBEAT.md, tests/test_soul_voice_input_section.py, tests/test_heartbeat_voice_input_note.py, tests/test_soul_dream_section.py
- Depends on: [voice-input-channel-integration], [neuroaffirming-personality], [state-response-integration]
- Issues: (1) LOW: description's literal "insert between `## Fire Tablet Display` and `## Calendar`" anchor wording is satisfied by ordering but not by adjacency — Task 16/17 already inserted sections in that range, and the implementation correctly refused to re-order. Future task descriptions should enumerate current neighbours when prescribing insertion slots. (2) LOW: scope deviation — `tests/test_soul_dream_section.py` constants bump is outside the description's strict four-file list. Mechanical (one int + one list insertion), documented in 19-03i.md Deviations, and authorised by that test file's own "bump the baseline with intent" pattern. No production code touched. (3) INFO: SOUL.md TL;DR at line 298 is one sentence ("Voice messages from Telegram are transcribed by Whisper and arrive as `[transcription: ...]` content.") vs the description's two-sentence suggestion; second sentence ("Treat them as normal text.") content is covered redundantly in `### How Voice Input Arrives`. No AC enforces the exact wording; test only checks the TL;DR contains `[transcription:`.
- Detail: _build/tasks/19-whisper-voice-input/sub-03/19-03x.md

### [voice-input-runbook-and-closure]
> Task 19 closure subtask — composes sub-01..03 into end-to-end evidence: a real local-faster-whisper backend transcribes a committed silent WAV fixture (with a Kokoro-regenerated-fixture branch ready for future use), a hosted-OpenAI path rides `httpx.MockTransport` through the sub-01 factory, the sub-02 `install_voice_transcription` patches a stub channel and routes the file through a recording backend, the `(low confidence) ` prefix assembles into the exact `[transcription: (low confidence) maybe hello]` string sub-03's SOUL.md teaches the LLM to recognise, the over-duration short-circuit confirms the backend is never called, and a backend exception resolves to `FAILED_MARKER` with the call counter proving the exception path was actually exercised. New `tests/test_voice_input_e2e.py` (289 lines, 7 tests + autouse `_reset_rate_limit_state` fixture, `mypy --strict` clean) — six pass, one (`test_local_backend_transcribes_fixture`) gates on `pytest.importorskip("faster_whisper")` plus a fixture-existence guard so it skips cleanly when the dep is absent (and on the build host's `.venv` it currently does); the local-backend assertion is an OR over (empty + low-confidence) and ("hello" substring) so a Kokoro-regenerated fixture passes without test edits. New `tests/fixtures/audio/hello_world.wav` (binary, 48 044 bytes — under the 50 KB cap with zero trim of sample rate / width) is 1.5 s of zero-PCM samples at 16 kHz mono 16-bit, generated by `scripts/generate_voice_fixture.py --mode=silent`. New `scripts/generate_voice_fixture.py` (136 lines — over the description's 80-line cap, see issue #1) is a one-shot CLI gated on `__name__ == "__main__"` with two modes: `--mode=silent` (default, what ships) writes the silent WAV via stdlib `wave`; `--mode=kokoro` synthesises arbitrary text via the project's `tts_engine.synthesize_speech` then resamples 24 kHz → 16 kHz via a stdlib decimating averager (3-in / 2-out preserving the speech band). Enforces a 50 KB cap at write-time (`SystemExit` if exceeded) so future regeneration that drifts above cap fails loud. New `workspace/VOICE_INPUT.md` (108 lines — under 180 cap, mirrors `MAC_DEPLOYMENT.md` / `INSTALL_MAC.md` conventions) ships eight required sections: TL;DR (5 bullets covering enable + send + reply + low-confidence + over-duration), How It Works (Telegram → temp path → patched `transcribe_audio` → backend → `[transcription: <text>]` upstream wrap → hook chain), Backends table (`local`/`openai`/`groq` × env-var / cost-per-min / privacy / latency rows; vendor-policy URLs pinned with retrieval date 2026-04-20), Switching Backends (5 copy-paste steps including `launchctl kickstart -k gui/$(id -u)/com.adhdassistant.bot` for Mac), When Transcription Is Wrong (4 bullets on confidence prefix / re-record / fall back to typing / pattern-report), When It's Silent (5-row troubleshooting matrix mapping symptom → likely cause → fix), Privacy (per-backend posture explicit), Cost (ties to the `$7/month` ceiling: local $0, openai ~$0.60/100msg, groq free-tier). New `tests/test_voice_input_md_deploy.py` (33 lines, mypy --strict clean) follows the five-test deploy pattern (3 manifest tests + 2 copy tests) verbatim from sibling `test_*_md_deploy.py` files. `setup_workspace.py` diff is exactly one line: `"VOICE_INPUT.md"` inserted into `TEMPLATE_FILES` between `"DASHBOARD.md"` and `"disco_voices.yaml"` — placement is thematic-grouped (user-facing surface docs) rather than alphabetical because the manifest is itself thematic, and the description sanctioned implementer judgement on the slot. Full Windows suite: 2027 → **2202 passed, 17 → 18 skipped** in 23.31 s — exactly +12 (11 pass + 1 skip) attributable to sub-04, zero regressions vs Task 18/sub-05 baseline. Cumulative Task 19 delta: +175 tests vs the 2027 baseline (52 sub-01 + 54 sub-02 + 58 sub-03 + 11 sub-04), well past the AC #6 cumulative `≥+55` floor and the AC #8 `≥2080 passed` floor. Scope discipline verified: zero edits to `gateway_runner.py` / `custom_gateway.py` / `hook_factory.py` / `start.py` / any `*_hook.py` / any engine module / any `references/` file / any `workspace/*.md` except the new `VOICE_INPUT.md`; zero new dependencies (sub-01's `faster-whisper>=1.0.0` already pinned in `requirements.txt`); zero new MCP servers, vendored trees, or `.env.example` entries. **Mac-deferred live smoke** (description AC #11): the real phone → Telegram voice → bot reply test runs on the Mac after this subtask lands; the runbook's "When It's Silent" matrix is the user's self-service path covering `[voice: /path/...]` (flag off), first-clip hang (model download), `failed to process voice message` (API key), `voice too long — limit 180s` (cap), and no-reply-at-all (channel offline, not voice). Task-level `task-verify.md` is explicitly OUT OF SCOPE here — owned by a separate Task-level verify-agent pass. Two commits: `f44386a` (runbook + e2e tests) plus impl/verify reports.
- Status: DONE
- Task: 19/sub-04
- Files: tests/test_voice_input_e2e.py, tests/test_voice_input_md_deploy.py, scripts/generate_voice_fixture.py, tests/fixtures/audio/hello_world.wav, workspace/VOICE_INPUT.md, setup_workspace.py
- Depends on: [whisper-transcription-backends], [voice-input-channel-integration], [voice-input-soul-guidance], [nanobot-workspace-setup], [voice-delivery-speak-tool], [tts-kokoro-engine]
- Issues: (1) LOW: `scripts/generate_voice_fixture.py` is 136 lines, exceeds the description's literal `≤80 lines` cap by 56. Cause is the `--mode=kokoro` branch (`synthesize_kokoro_wav_bytes` + `downsample_24k_to_16k` + `write_kokoro_resampled_wav` ≈ 50 lines); silent path alone is ~30 lines. Script is gated on `__name__ == "__main__"` and never imported from production. Fix options: (a) drop `--kokoro` from the committed file (silent is what ships); (b) split Kokoro path to `scripts/generate_voice_fixture_kokoro.py`; (c) amend the description cap. (2) LOW: `tests/test_voice_input_e2e.py:145-169` (`test_hosted_openai_path_with_mock_transport`) deviates from the literal AC #1 wording "asserts the `render_transcription_text` output is identical whether content hit the Whisper API or the mock (contract stability)" — the test instead asserts factory-built `isinstance(OpenAIWhisperBackend)` plus directly-built shape (`text`/`confidence is None`/`backend_name`). Cause: `build_transcription_backend` lacks a `transport` parameter, so a literal "factory + mock" run is impossible without inventing a seam. Impl report §"Hosted-backend test" documents the trade-off; the contract spirit (factory dispatches correctly + serialisation produces the right `TranscriptionResult`) is preserved. Fix (optional): add `transport: AsyncBaseTransport | None = None` to the openai/groq factory branches. (3) LOW: `tests/test_voice_input_e2e.py:51` autouse fixture calls `voice_input_integration._reset_rate_limit_state()` — cross-module access to an underscore-private name. The seam exists in production at `voice_input_integration.py:139` precisely for this purpose; rename to `reset_rate_limit_state_for_tests` (or expose a public wrapper) for clarity. (4) LOW: `tests/test_voice_input_e2e.py:272-289` (`test_backend_exception_returns_failed_marker`) lacks an explicit `with does_not_raise():` — propagation guarantee is implicit (the test would raise from `_run` if the exception escaped, and the `assert` would never fire), but the AC text "exception does NOT propagate" is not literally checked. Add a wrapper or an inline comment naming the implicit invariant. (5) INFO: `setup_workspace.py:80` slot deviates from description AC #4's "between SYNCALL.md and USER.md" anchor — placed after `DASHBOARD.md` because the manifest is in fact thematically grouped, not alphabetical. Description's own "implementer verifies the sort order" sanction covers the deviation; flagging only because future readers may compare description vs code. (6) INFO: `mypy --strict` on `scripts/generate_voice_fixture.py` reports 3 errors, all in `tts_engine.py` (pre-existing `kokoro_onnx` stub-missing + `attr-defined` from sub-04's perspective); the generator script itself is clean. Errors surface only because mypy follows the local `from tts_engine import ...` line inside the `--kokoro` path. Acknowledged in impl report; not a sub-04 regression.
- Detail: _build/tasks/19-whisper-voice-input/sub-04/19-04x.md
